{
  "goals": [
    "Expose all existing `parallax` CLI commands as MCP tools inside `@parallax/mcp`.",
    "Each tool invokes the `parallax` CLI binary as a subprocess via `Bun.spawn`, forwarding arguments built from the tool's input schema.",
    "Return the output file path (and any relevant metadata) to the calling AI client upon success.",
    "The MCP server is startable via `bun run start` in `packages/parallax_mcp/` and registerable in any MCP-compatible AI client config."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Create Image via MCP Tool",
      "description": "As a developer using an AI client (e.g. Claude Desktop), I want to call a `create_image` MCP tool so that the AI client can generate an image by invoking `parallax create image` without me running CLI commands manually.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "An MCP tool named `create_image` is registered on the server with an input schema matching `parallax create image` options: `model` (required), `prompt` (required), `negativePrompt`, `width`, `height`, `steps`, `cfg`, `seed`, `output`, `modelsDir`."
        },
        {
          "id": "US-001-AC02",
          "text": "Invoking the tool spawns `bun run src/index.ts create image --model <model> --prompt <prompt> [...]` via `Bun.spawn` inside `@parallax/cli`."
        },
        {
          "id": "US-001-AC03",
          "text": "On success, the tool result includes the resolved output file path."
        },
        {
          "id": "US-001-AC04",
          "text": "On failure (non-zero exit code), the tool result includes the stderr output as an error message."
        },
        {
          "id": "US-001-AC05",
          "text": "Typecheck / lint passes."
        },
        {
          "id": "US-001-AC06",
          "text": "Visually verified: Claude Desktop (or equivalent) can call `create_image` and receive the output path."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Create Video via MCP Tool",
      "description": "As a developer using an AI client, I want to call a `create_video` MCP tool so that the AI client can trigger `parallax create video` and receive the output path.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "An MCP tool named `create_video` is registered with input schema matching `parallax create video` options: `model` (required), `prompt` (required), `input`, `width`, `height`, `length`, `steps`, `cfg`, `seed`, `output`, `modelsDir`."
        },
        {
          "id": "US-002-AC02",
          "text": "Spawns the CLI subprocess correctly and returns output file path on success or stderr on failure."
        },
        {
          "id": "US-002-AC03",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Create Audio via MCP Tool",
      "description": "As a developer using an AI client, I want to call a `create_audio` MCP tool so that the AI client can trigger `parallax create audio`.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "An MCP tool named `create_audio` is registered with input schema matching `parallax create audio` options: `model` (required), `prompt` (required), `length`, `steps`, `cfg`, `bpm`, `lyrics`, `seed`, `output`, `modelsDir`."
        },
        {
          "id": "US-003-AC02",
          "text": "Spawns the CLI subprocess and returns output file path on success or stderr on failure."
        },
        {
          "id": "US-003-AC03",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "Edit Image via MCP Tool",
      "description": "As a developer using an AI client, I want to call an `edit_image` MCP tool so that the AI client can trigger `parallax edit image`.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "An MCP tool named `edit_image` is registered with input schema matching `parallax edit image` options: `model` (required), `prompt` (required), `input` (required), `subjectImage`, `width`, `height`, `steps`, `cfg`, `seed`, `output`, `image2`, `image3`, `noLora`, `modelsDir`."
        },
        {
          "id": "US-004-AC02",
          "text": "Spawns the CLI subprocess and returns output file path on success or stderr on failure."
        },
        {
          "id": "US-004-AC03",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-005",
      "title": "Upscale Image via MCP Tool",
      "description": "As a developer using an AI client, I want to call an `upscale_image` MCP tool so that the AI client can trigger `parallax upscale image`.",
      "acceptanceCriteria": [
        {
          "id": "US-005-AC01",
          "text": "An MCP tool named `upscale_image` is registered with input schema matching `parallax upscale image` options: `model` (required), `prompt` (required), `input` (required), `checkpoint`, `esrganCheckpoint`, `latentUpscaleCheckpoint`, `negativePrompt`, `width`, `height`, `steps`, `cfg`, `seed`, `output`, `outputBase`, `modelsDir`."
        },
        {
          "id": "US-005-AC02",
          "text": "Spawns the CLI subprocess and returns output file path on success or stderr on failure."
        },
        {
          "id": "US-005-AC03",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-006",
      "title": "MCP Server Startup and Client Registration",
      "description": "As a developer, I want to start the MCP server with `bun run start` and register it in my AI client config so that the tools become available to the AI client.",
      "acceptanceCriteria": [
        {
          "id": "US-006-AC01",
          "text": "`bun run start` in `packages/parallax_mcp/` starts the server without errors."
        },
        {
          "id": "US-006-AC02",
          "text": "The server responds to MCP `tools/list` with all 5 tools (`create_image`, `create_video`, `create_audio`, `edit_image`, `upscale_image`)."
        },
        {
          "id": "US-006-AC03",
          "text": "The README or inline comment documents the config snippet needed to register the server in Claude Desktop / GitHub Copilot."
        },
        {
          "id": "US-006-AC04",
          "text": "Typecheck / lint passes."
        },
        {
          "id": "US-006-AC05",
          "text": "Visually verified: AI client config registers the server and tools appear in the client."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "All MCP tools must be registered in `packages/parallax_mcp/src/index.ts` using `server.tool()` from `@modelcontextprotocol/sdk`."
    },
    {
      "id": "FR-2",
      "description": "Subprocess invocation must use `Bun.spawn` pointing at the `@parallax/cli` entry point (`packages/parallax_cli/src/index.ts`), not a compiled binary, to avoid build prerequisites."
    },
    {
      "id": "FR-3",
      "description": "Tool input schemas must use `zod` (or the SDK's built-in schema helpers) and must match the CLI flag names (camelCase mapping to kebab-case flags)."
    },
    {
      "id": "FR-4",
      "description": "Boolean flags (e.g. `noLora`) must be conditionally appended — only when `true` — since the CLI uses presence-based flags."
    },
    {
      "id": "FR-5",
      "description": "The tool result on success must be a text content block containing at minimum the resolved output file path."
    },
    {
      "id": "FR-6",
      "description": "The tool result on failure must be a text content block containing the process stderr, and `isError: true`."
    },
    {
      "id": "FR-7",
      "description": "No new top-level dependencies beyond what is already in `packages/parallax_mcp/package.json` unless strictly necessary (prefer `zod` which is likely already available via the SDK)."
    }
  ]
}
