{
  "goals": [
    "Provide a self-contained, executable CLI example for `edit_2511` that a developer can run",
    "Document the pipeline's public API (`manifest()` + `run()`) through concrete, annotated"
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Download models via CLI",
      "description": "As a developer integrating `comfy_diffusion`, I want to run the example with `--download-only` so that I can fetch all required model weights before running inference, without modifying any code.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`uv run python examples/image/edit/qwen/edit_2511.py --models-dir /path/to/models --image placeholder.png --prompt \"x\" --download-only` exits with code `0`."
        },
        {
          "id": "US-001-AC02",
          "text": "All four model files listed in `manifest()` are downloaded (or skipped if present)."
        },
        {
          "id": "US-001-AC03",
          "text": "The script prints a clear \"Models ready.\" message and exits without performing inference."
        },
        {
          "id": "US-001-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Edit an image with the Lightning LoRA (default, 4 steps)",
      "description": "As a developer, I want to pass `--image`, `--prompt`, and (optionally) `--seed` so that I get an edited output image saved to disk using the fast 4-step Lightning LoRA path.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "`uv run python examples/image/edit/qwen/edit_2511.py --models-dir /path/to/models --image input.png --prompt \"Make the sofa look like it is covered in fur\"` completes without error."
        },
        {
          "id": "US-002-AC02",
          "text": "The output image is saved as `<output-prefix>.png` (default prefix: `qwen_edit_2511_output`)."
        },
        {
          "id": "US-002-AC03",
          "text": "Default `steps=4` and `use_lora=True` are used when `--no-lora` is not passed."
        },
        {
          "id": "US-002-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Edit without Lightning LoRA (40 steps)",
      "description": "As a developer, I want to pass `--no-lora` so that I can run the standard 40-step CFGNorm path when higher quality is preferred over speed.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "Adding `--no-lora` sets `use_lora=False` and `steps=40` in the `run()` call."
        },
        {
          "id": "US-003-AC02",
          "text": "The script accepts an optional explicit `--steps` override that takes precedence over the LoRA-derived default."
        },
        {
          "id": "US-003-AC03",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "Multi-reference image editing",
      "description": "As a developer, I want to pass `--image2` and/or `--image3` so that I can provide additional reference images for the editing conditioning.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "`--image2` and `--image3` are optional CLI arguments; both default to `None`."
        },
        {
          "id": "US-004-AC02",
          "text": "When provided, each path is validated (`is_file()`) before being loaded and passed to `run()`."
        },
        {
          "id": "US-004-AC03",
          "text": "When not provided, `None` is passed to `run()` (no error)."
        },
        {
          "id": "US-004-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "** The example file is located at `examples/image/edit/qwen/edit_2511.py`."
    },
    {
      "id": "FR-2",
      "description": "** The script uses `argparse` with the following arguments:"
    },
    {
      "id": "FR-3",
      "description": "** When `--models-dir` is not set and `PYCOMFY_MODELS_DIR` is not defined, the"
    },
    {
      "id": "FR-4",
      "description": "** Default steps: `4` when `use_lora=True`; `40` when `use_lora=False`.  An"
    },
    {
      "id": "FR-5",
      "description": "** Output image is saved as `{output_prefix}.png` using `PIL.Image.save()`."
    },
    {
      "id": "FR-6",
      "description": "** The module-level docstring mirrors the format used in `layered_i2l.py`:"
    },
    {
      "id": "FR-7",
      "description": "** `check_runtime()` is called after model download and before inference; error"
    },
    {
      "id": "FR-8",
      "description": "** All imports from `comfy_diffusion` are deferred to inside `main()` (after"
    }
  ]
}
