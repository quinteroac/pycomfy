{
  "goals": [
    "Implement a Python MCP server under `mcp/` using `fastmcp`.",
    "Migrate all five inference tools to return a job ID within 200ms instead of blocking.",
    "Add `get_job_status` and `wait_for_job` tools.",
    "Register the server as a `uv run` entry point in `pyproject.toml`."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Non-blocking inference tools",
      "description": "As an AI agent using the MCP server, I want `create_image`, `create_video`, `create_audio`, `edit_image`, and `upscale_image` to return a job ID immediately so that I can avoid tool timeouts on long-running operations and continue reasoning while inference runs.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "All five tools call `submit_job()` from `server/submit.py` instead of blocking."
        },
        {
          "id": "US-001-AC02",
          "text": "Each tool returns within 200ms with a text response of the form: `job_id: <id>\\nstatus: queued\\nmodel: <model>`."
        },
        {
          "id": "US-001-AC03",
          "text": "The tool `description` fields mention that they return a job ID and that `wait_for_job` should be used to get the output path."
        },
        {
          "id": "US-001-AC04",
          "text": "A unit test verifies that each tool returns a string containing `job_id:` in under 500ms (with a mock queue)."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "`get_job_status` tool",
      "description": "As an AI agent, I want to call `get_job_status(job_id)` so that I can check whether a job is still running, completed, or failed without blocking my reasoning loop.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "`get_job_status` accepts `job_id: str` as its only parameter."
        },
        {
          "id": "US-002-AC02",
          "text": "Returns a text response with fields: `status`, `model`, `created_at`, and `output_path` (if completed) or `error` (if failed)."
        },
        {
          "id": "US-002-AC03",
          "text": "Returns `status: not_found` (not an error/exception) when the job ID does not exist in the queue."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "`wait_for_job` tool",
      "description": "As an AI agent, I want to call `wait_for_job(job_id, timeout_seconds)` so that I can block my current tool call until the job finishes and get the output path directly.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "`wait_for_job` accepts `job_id: str` and `timeout_seconds: int` (default 600)."
        },
        {
          "id": "US-003-AC02",
          "text": "Polls the job queue every 2 seconds until the job status is `\"completed\"` or `\"failed\"`."
        },
        {
          "id": "US-003-AC03",
          "text": "On `\"completed\"`: returns `output: <path>` as text response."
        },
        {
          "id": "US-003-AC04",
          "text": "On `\"failed\"`: returns `error: <message>` as text response."
        },
        {
          "id": "US-003-AC05",
          "text": "On timeout: returns `error: timeout after <N>s` as text response (does not raise)."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "MCP server entry point",
      "description": "As a developer configuring Claude Desktop or another MCP client, I want to point the client at `uv run parallax-mcp` so that the Python MCP server starts as a stdio process without needing a separate Bun install.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "`mcp/__main__.py` exists and starts the `fastmcp` server in stdio mode."
        },
        {
          "id": "US-004-AC02",
          "text": "`pyproject.toml` registers a `parallax-mcp` script entry point pointing to `mcp.main:main`."
        },
        {
          "id": "US-004-AC03",
          "text": "`uv run parallax-mcp` starts without error (no GPU required — tools return early with mock job IDs in test mode)."
        },
        {
          "id": "US-004-AC04",
          "text": "The server name reported to MCP clients is `\"parallax-mcp\"` with version matching `pyproject.toml`."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "MCP server is implemented with `fastmcp`; `mcp/` directory at repo root contains `main.py`, `tools/`, and `__main__.py`."
    },
    {
      "id": "FR-2",
      "description": "All tools import `submit_job` and `get_queue` from `server/submit.py` and `server/queue.py` — no direct `comfy.*` imports in `mcp/`."
    },
    {
      "id": "FR-3",
      "description": "Tool input schemas are defined as Pydantic models or typed function signatures (fastmcp auto-generates the JSON schema)."
    },
    {
      "id": "FR-4",
      "description": "The MCP server runs as a stdio process (`fastmcp` stdio transport) — not HTTP."
    },
    {
      "id": "FR-5",
      "description": "`wait_for_job` uses `asyncio.sleep(2)` between polls — never busy-waits."
    },
    {
      "id": "FR-6",
      "description": "All tool docstrings are present and descriptive (fastmcp uses them as tool descriptions)."
    }
  ]
}
