{
  "goals": [
    "Introduce a persistent, file-based job queue (bunqueue + SQLite) in `parallax_sdk` accessible by all packages.",
    "Provide a `submitJob()` function that enqueues a job and spawns a detached worker process, returning a job ID in < 100ms.",
    "Implement the worker process (`packages/parallax_cli/src/_run.ts`) that picks up one job, runs the Python pipeline, reads NDJSON progress from stdout, and updates the job record.",
    "Add a Python `ProgressReporter` helper so existing pipelines can emit structured progress without rewriting their core logic."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Define shared job types",
      "description": "As a TypeScript developer on any parallax package, I want well-typed `JobData` and `JobResult` interfaces in `@parallax/sdk` so that I can submit and read jobs without casting or guessing shapes.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`packages/parallax_sdk/src/jobs.ts` exports `ParallaxJobData`, `ParallaxJobResult`, and `PythonProgress` interfaces matching the shapes defined in the design doc."
        },
        {
          "id": "US-001-AC02",
          "text": "`ParallaxJobData` includes: `action`, `media`, `model`, `script`, `args`, `scriptBase`, `uvPath`."
        },
        {
          "id": "US-001-AC03",
          "text": "`ParallaxJobResult` includes: `outputPath: string`."
        },
        {
          "id": "US-001-AC04",
          "text": "`PythonProgress` includes: `step: string`, `pct: number`, and optional `frame`, `total`, `output`, `error`."
        },
        {
          "id": "US-001-AC05",
          "text": "Types are re-exported from `packages/parallax_sdk/src/index.ts`."
        },
        {
          "id": "US-001-AC06",
          "text": "`bun typecheck` passes with no errors in `parallax_sdk`."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Bunqueue singleton in parallax_sdk",
      "description": "As a parallax package, I want to call `getQueue()` and get a shared bunqueue instance backed by `~/.config/parallax/jobs.db` so that all packages read and write to the same SQLite job store without owning the queue lifecycle themselves.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "`packages/parallax_sdk/src/queue.ts` exports `getQueue(): Bunqueue`."
        },
        {
          "id": "US-002-AC02",
          "text": "`getQueue()` is a lazy singleton — creates the instance on first call, returns the same instance on subsequent calls within a process."
        },
        {
          "id": "US-002-AC03",
          "text": "The SQLite database path resolves to `~/.config/parallax/jobs.db` (uses `os.homedir()`)."
        },
        {
          "id": "US-002-AC04",
          "text": "Queue is created in embedded mode (`embedded: true`) with no processor — consumers attach their own processors."
        },
        {
          "id": "US-002-AC05",
          "text": "`bunqueue` is added as a dependency to `packages/parallax_sdk/package.json`."
        },
        {
          "id": "US-002-AC06",
          "text": "`bun typecheck` passes."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "submitJob() helper",
      "description": "As a CLI command or API handler, I want to call `submitJob(data)` and receive a job ID immediately so that I can return the ID to the user without waiting for inference to finish.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "`packages/parallax_sdk/src/submit.ts` exports `submitJob(data: ParallaxJobData): Promise<string>` returning the job ID."
        },
        {
          "id": "US-003-AC02",
          "text": "`submitJob` enqueues a job named `\"pipeline\"` via `getQueue().add(...)` with `attempts: 1` and `timeout: 30 * 60 * 1000` (30 min)."
        },
        {
          "id": "US-003-AC03",
          "text": "After enqueuing, `submitJob` spawns a detached Bun process: `bun packages/parallax_cli/src/_run.ts <jobId>` with `stdin: \"ignore\"`, `stdout: \"ignore\"`, `stderr: \"ignore\"`, `detached: true`."
        },
        {
          "id": "US-003-AC04",
          "text": "`submitJob` closes the queue connection before returning."
        },
        {
          "id": "US-003-AC05",
          "text": "The full call (enqueue + spawn) completes in under 200ms on a cold start."
        },
        {
          "id": "US-003-AC06",
          "text": "`bun typecheck` passes."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "Detached worker process (_run.ts)",
      "description": "As a submitted job, I want a dedicated worker process to pick me up, run the Python pipeline, stream NDJSON progress into the job record, and mark me completed or failed so that the submitting process can exit without losing my execution.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "`packages/parallax_cli/src/_run.ts` reads `process.argv[2]` as the job ID."
        },
        {
          "id": "US-004-AC02",
          "text": "Creates a bunqueue instance with a `\"pipeline\"` route processor."
        },
        {
          "id": "US-004-AC03",
          "text": "Spawns the Python pipeline with `Bun.spawn([uvPath, \"run\", \"python\", join(scriptBase, script), ...args], { stdout: \"pipe\", stderr: \"pipe\" })`."
        },
        {
          "id": "US-004-AC04",
          "text": "Reads stdout line by line; for each valid JSON line matching `PythonProgress`, calls `job.updateProgress(event.pct)`."
        },
        {
          "id": "US-004-AC05",
          "text": "On successful exit, returns `{ outputPath }` as the job result."
        },
        {
          "id": "US-004-AC06",
          "text": "On non-zero exit code, throws an `Error` with the message `Pipeline failed (exit <code>)`."
        },
        {
          "id": "US-004-AC07",
          "text": "Closes the queue once the job is `completed` or `failed`."
        },
        {
          "id": "US-004-AC08",
          "text": "Subprocess stderr is captured and included in the failure message when the pipeline exits non-zero."
        },
        {
          "id": "US-004-AC09",
          "text": "`bun typecheck` passes."
        }
      ]
    },
    {
      "id": "US-005",
      "title": "Python ProgressReporter helper",
      "description": "As a Python pipeline author, I want to call `progress(step, pct, kwargs)` so that the worker process receives structured progress without me rewriting the pipeline's inference logic.",
      "acceptanceCriteria": [
        {
          "id": "US-005-AC01",
          "text": "`packages/parallax_cli/runtime/progress.py` exports a `progress(step: str, pct: float, **kwargs) -> None` function."
        },
        {
          "id": "US-005-AC02",
          "text": "The function prints `json.dumps({\"step\": step, \"pct\": pct, **kwargs})` followed by a newline to stdout, with `flush=True`."
        },
        {
          "id": "US-005-AC03",
          "text": "At minimum, the following pipeline files are updated to call `progress()` at logical milestones (download, model load, sampling start, sampling per-frame or per-step, encode, done): `runtime/video/ltx/ltx2/t2v.py` and `runtime/video/wan/wan22/t2v.py`."
        },
        {
          "id": "US-005-AC04",
          "text": "Updated pipelines still print the final output path as the last line (unchanged behavior for the sync path)."
        },
        {
          "id": "US-005-AC05",
          "text": "No changes to pipeline core logic (model loading, sampling, saving)."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "The SQLite database file is created automatically on first use if it does not exist."
    },
    {
      "id": "FR-2",
      "description": "The detached worker process must not inherit the parent's stdio file descriptors."
    },
    {
      "id": "FR-3",
      "description": "Non-JSON lines emitted by the Python pipeline to stdout must be silently ignored by the worker (no crash)."
    },
    {
      "id": "FR-4",
      "description": "All new TypeScript modules in `parallax_sdk` must be re-exported from `src/index.ts`."
    },
    {
      "id": "FR-5",
      "description": "The `bunqueue` package version must be pinned to a specific minor version in `package.json` (no `^` or `~` floating)."
    }
  ]
}
