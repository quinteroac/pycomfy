{
  "goals": [
    "Serve the pre-built frontend from the FastAPI server with zero additional processes.",
    "Avoid breaking existing API routes.",
    "Make the frontend path configurable so `parallax frontend install` (PRD 003) can point to a custom directory."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Access the frontend via the FastAPI server",
      "description": "As a user, I want to open `http://localhost:5000/ui` in a browser and see the Parallax chat interface so that I do not need to run a separate static file server.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "Navigating to `http://localhost:5000/ui` returns `index.html` from the mounted frontend directory."
        },
        {
          "id": "US-001-AC02",
          "text": "All hashed JS and CSS assets referenced by `index.html` are served correctly from `/ui/assets/`."
        },
        {
          "id": "US-001-AC03",
          "text": "Navigating to `http://localhost:5000/ui/` (trailing slash) also returns `index.html`."
        },
        {
          "id": "US-001-AC04",
          "text": "Existing API routes (`/create/*`, `/jobs/*`) continue to respond correctly — no route collision with `/ui`."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Configure the frontend directory path",
      "description": "As an operator, I want the frontend directory to be configurable via an environment variable so that `parallax frontend install` can point the server to the installed location without modifying source code.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "The server reads `PARALLAX_FRONTEND_PATH` from the environment."
        },
        {
          "id": "US-002-AC02",
          "text": "If `PARALLAX_FRONTEND_PATH` is set and the path exists, the server mounts it at `/ui`."
        },
        {
          "id": "US-002-AC03",
          "text": "If `PARALLAX_FRONTEND_PATH` is not set, the server falls back to `frontend/dist/` relative to the repo root (development default)."
        },
        {
          "id": "US-002-AC04",
          "text": "If neither path exists, the server starts normally without mounting `/ui` and logs a warning: `\"Frontend not found at {path} — /ui will not be served.\"`"
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "Mount the frontend directory using FastAPI's `StaticFiles` at the `/ui` prefix."
    },
    {
      "id": "FR-2",
      "description": "The `index.html` is served for the root of the mount (`/ui` and `/ui/`)."
    },
    {
      "id": "FR-3",
      "description": "`PARALLAX_FRONTEND_PATH` env var controls the mounted directory; fallback is `frontend/dist/` relative to the server working directory."
    },
    {
      "id": "FR-4",
      "description": "The mount is added in `server/main.py` after all API routers are registered, so API routes take precedence."
    },
    {
      "id": "FR-5",
      "description": "The server must not crash at startup if the frontend directory is absent — it logs a warning and skips the mount."
    }
  ]
}
