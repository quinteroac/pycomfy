{
  "goals": [
    "`pip install comfy-diffusion` produces a working installation without requiring `git submodule init`.",
    "`check_runtime()` remains the single entry point for runtime validation and bootstrap; callers never need to know about the download.",
    "Zero new runtime dependencies — only Python stdlib (`urllib.request`, `zipfile`, `shutil`, `pathlib`)."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Auto-detect missing ComfyUI and download it",
      "description": "As a developer who installed `comfy-diffusion` via `pip install`, I want `check_runtime()` to automatically download ComfyUI when `vendor/ComfyUI` is missing or empty so that I do not need to manually run `git submodule init && git submodule update`.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`check_runtime()` checks whether `vendor/ComfyUI` exists and contains at least one file (e.g. `comfy/` subdirectory present)."
        },
        {
          "id": "US-001-AC02",
          "text": "If the directory is missing or empty, `check_runtime()` downloads the pinned ComfyUI release zip from GitHub using `urllib.request` and extracts it into `vendor/ComfyUI` using `zipfile`."
        },
        {
          "id": "US-001-AC03",
          "text": "The downloaded version matches the same tag/commit that the git submodule is pinned to (hardcoded constant in `_runtime.py`)."
        },
        {
          "id": "US-001-AC04",
          "text": "After a successful download, `check_runtime()` returns the same healthy dict as if the submodule had been initialized manually."
        },
        {
          "id": "US-001-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Return error dict on bootstrap failure",
      "description": "As a developer, I want `check_runtime()` to return a structured error dict when the auto-download fails so that my application can surface a clear message without crashing.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "If the download or extraction raises any exception (network error, permission denied, disk full, etc.), `check_runtime()` catches it and returns an error dict with key `\"error\"` containing a human-readable description of the failure."
        },
        {
          "id": "US-002-AC02",
          "text": "`check_runtime()` never raises an exception regardless of bootstrap outcome."
        },
        {
          "id": "US-002-AC03",
          "text": "The error dict includes `\"python_version\"` (already required by existing contract)."
        },
        {
          "id": "US-002-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Test bootstrap with empty vendor directory",
      "description": "As a developer, I want a pytest test that simulates a missing `vendor/ComfyUI` and calls `check_runtime()` so that the bootstrap path is automatically validated on every CI run.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "A test in `tests/` temporarily renames or mocks `vendor/ComfyUI` to simulate an empty/absent state, calls `check_runtime()`, and asserts the returned dict contains no `\"error\"` key after bootstrap succeeds."
        },
        {
          "id": "US-003-AC02",
          "text": "The test does not make real network calls — it patches `urllib.request.urlretrieve` (or equivalent) to provide a minimal valid zip fixture."
        },
        {
          "id": "US-003-AC03",
          "text": "Test passes on CPU-only CI environment without GPU."
        },
        {
          "id": "US-003-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "Update documentation to instruct users to call `check_runtime()` first",
      "description": "As a developer reading the docs, I want the README (or equivalent entry-point documentation) to explicitly state that `check_runtime()` must be called before any other `comfy_diffusion` API so that I know the bootstrap will be triggered automatically.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "`README.md` (or the primary usage doc) contains a \"Quick Start\" or \"Usage\" section showing `check_runtime()` as the first call before any model loading or inference."
        },
        {
          "id": "US-004-AC02",
          "text": "The section explains that calling `check_runtime()` on first use triggers an automatic download of ComfyUI if needed."
        },
        {
          "id": "US-004-AC03",
          "text": "The section shows how to handle the error dict returned on failure."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "`_runtime.py` must define a module-level constant `COMFYUI_PINNED_TAG` (e.g. `\"v0.3.43\"`) that identifies the ComfyUI release to download."
    },
    {
      "id": "FR-2",
      "description": "The download URL must be derived from `COMFYUI_PINNED_TAG` pointing to the GitHub archive zip (e.g. `https://github.com/comfyanonymous/ComfyUI/archive/refs/tags/{tag}.zip`)."
    },
    {
      "id": "FR-3",
      "description": "Extraction must place ComfyUI contents directly under `vendor/ComfyUI/` (strip the top-level archive directory created by GitHub's zip format)."
    },
    {
      "id": "FR-4",
      "description": "Bootstrap must be idempotent — if `vendor/ComfyUI` is already populated, `check_runtime()` skips the download entirely."
    },
    {
      "id": "FR-5",
      "description": "Only Python stdlib modules may be used for network and archive operations (`urllib.request`, `zipfile`, `shutil`, `pathlib`, `tempfile`)."
    },
    {
      "id": "FR-6",
      "description": "`check_runtime()` must always populate `\"python_version\"` in the returned dict, whether bootstrap succeeds, fails, or is skipped."
    }
  ]
}
