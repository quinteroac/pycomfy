{
  "goals": [
    "Allow any end user to launch the ComfyUI web interface with a single command.",
    "Provide lifecycle management (start, stop, status) consistent with the existing `parallax ms` service pattern.",
    "Support port customisation and optional browser auto-open out of the box."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Start ComfyUI",
      "description": "As a developer, I want to run `parallax comfyui start` so that ComfyUI's web UI starts and is accessible in my browser without manual setup.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "Running `parallax comfyui start` launches ComfyUI as a background process using the runtime at `~/.parallax/env`."
        },
        {
          "id": "US-001-AC02",
          "text": "The process listens on port `8188` by default."
        },
        {
          "id": "US-001-AC03",
          "text": "A PID file is written to `~/.config/parallax/comfyui.pid` so the process can be tracked."
        },
        {
          "id": "US-001-AC04",
          "text": "The command prints the URL (`http://localhost:<port>`) once the server is ready."
        },
        {
          "id": "US-001-AC05",
          "text": "If ComfyUI is already running, the command prints a warning and exits without starting a second instance."
        },
        {
          "id": "US-001-AC06",
          "text": "Typecheck / lint passes."
        },
        {
          "id": "US-001-AC07",
          "text": "Visually verified in terminal: output shows the URL and process starts cleanly."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Stop ComfyUI",
      "description": "As a developer, I want to run `parallax comfyui stop` so that the running ComfyUI process is cleanly terminated.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "Running `parallax comfyui stop` reads the PID from `~/.config/parallax/comfyui.pid` and terminates the process."
        },
        {
          "id": "US-002-AC02",
          "text": "The PID file is removed after a successful stop."
        },
        {
          "id": "US-002-AC03",
          "text": "If no instance is running, the command prints an informative message and exits with code 0."
        },
        {
          "id": "US-002-AC04",
          "text": "Works cross-platform: `SIGTERM` on Linux/macOS, `TerminateProcess` on Windows."
        },
        {
          "id": "US-002-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Override Port with `--port`",
      "description": "As a developer, I want to pass `--port <N>` to `parallax comfyui start` so that I can run ComfyUI on a port other than `8188` (e.g. when the default is occupied).",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "`parallax comfyui start --port 8189` starts ComfyUI on port `8189`."
        },
        {
          "id": "US-003-AC02",
          "text": "The printed URL reflects the custom port."
        },
        {
          "id": "US-003-AC03",
          "text": "Passing an invalid port (< 1 or > 65535) prints an error and exits with a non-zero code."
        },
        {
          "id": "US-003-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "Auto-open Browser with `--open`",
      "description": "As a developer, I want to pass `--open` to `parallax comfyui start` so that my default browser opens the ComfyUI URL automatically after the server is ready.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "`parallax comfyui start --open` opens `http://localhost:<port>` in the default browser once the server responds on the port."
        },
        {
          "id": "US-004-AC02",
          "text": "Uses `webbrowser.open()` (stdlib) — no additional dependency."
        },
        {
          "id": "US-004-AC03",
          "text": "Works on Linux, macOS, and Windows."
        },
        {
          "id": "US-004-AC04",
          "text": "Typecheck / lint passes."
        },
        {
          "id": "US-004-AC05",
          "text": "Visually verified in browser: ComfyUI interface loads."
        }
      ]
    },
    {
      "id": "US-005",
      "title": "Check Status with `parallax comfyui status`",
      "description": "As a developer, I want to run `parallax comfyui status` so that I can confirm whether a ComfyUI instance is currently running and on which port.",
      "acceptanceCriteria": [
        {
          "id": "US-005-AC01",
          "text": "If running: prints `ComfyUI is running (PID <pid>, port <port>)`."
        },
        {
          "id": "US-005-AC02",
          "text": "If not running: prints `ComfyUI is not running` and exits with code 0."
        },
        {
          "id": "US-005-AC03",
          "text": "Port is read from the PID file metadata (stored alongside the PID at start time)."
        },
        {
          "id": "US-005-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "A new Typer command group `comfyui` is registered in `cli/main.py` with subcommands `start`, `stop`, and `status`."
    },
    {
      "id": "FR-2",
      "description": "Implementation lives in `cli/commands/comfyui.py` (mirrors `cli/commands/ms.py` structure)."
    },
    {
      "id": "FR-3",
      "description": "`start` launches ComfyUI via the runtime Python at `~/.parallax/env` using `subprocess.Popen` (non-blocking); the working directory is set to `vendor/ComfyUI` relative to the installed package."
    },
    {
      "id": "FR-4",
      "description": "The PID file at `~/.config/parallax/comfyui.pid` stores `pid:<N>\\nport:<P>` so `status` and `stop` can read both values without re-scanning."
    },
    {
      "id": "FR-5",
      "description": "`start` polls the port (TCP connect) for up to 30 seconds before printing the ready URL; polling interval is 0.5 s."
    },
    {
      "id": "FR-6",
      "description": "All file paths use `pathlib.Path` and respect `PARALLAX_DB_PATH`-style conventions (config dir derived from `platformdirs` or `~/.config/parallax` fallback)."
    },
    {
      "id": "FR-7",
      "description": "Cross-platform process termination: `os.kill(pid, signal.SIGTERM)` on POSIX; `subprocess.call(['taskkill', '/F', '/PID', str(pid)])` on Windows."
    },
    {
      "id": "FR-8",
      "description": "`start` redirects ComfyUI stdout and stderr to `~/.config/parallax/comfyui.log` (append mode). The ready message printed to the terminal includes the log path so the user knows where to find it."
    }
  ]
}
