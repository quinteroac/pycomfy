{
  "goals": [
    "Reduce onboarding to a single command: `curl -fsSL https://raw.githubusercontent.com/quinteroac/comfy-diffusion/master/install.sh | sh`.",
    "Handle platform/arch detection, download, checksum verification, and PATH setup automatically.",
    "Provide clear, human-readable output at each step for a non-developer audience."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Install parallax on Linux or macOS with one command",
      "description": "As a non-developer on Linux or macOS, I want to run `curl -fsSL https://raw.githubusercontent.com/quinteroac/comfy-diffusion/master/install.sh | sh` so that `parallax` is available in my terminal without any manual steps.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "The script detects OS (`uname -s`) and architecture (`uname -m`) and selects the correct asset name: `parallax-linux-x86_64` for Linux x86_64, `parallax-macos-universal` for macOS (any arch)."
        },
        {
          "id": "US-001-AC02",
          "text": "The script queries `https://api.github.com/repos/quinteroac/comfy-diffusion/releases/latest` to obtain the latest tag; if `PARALLAX_VERSION` is set in the environment, it uses that version instead."
        },
        {
          "id": "US-001-AC03",
          "text": "The script downloads the binary and its `.sha256` file from the GitHub Release using `curl` (preferred) or `wget` (fallback), with a progress indicator."
        },
        {
          "id": "US-001-AC04",
          "text": "The script verifies the downloaded binary against the `.sha256` checksum using `sha256sum` (Linux) or `shasum -a 256` (macOS); if verification fails, it deletes the file, prints \"Checksum verification failed. Aborting.\", and exits with code 1."
        },
        {
          "id": "US-001-AC05",
          "text": "The script installs the binary to `~/.local/bin/parallax` (creating the directory if needed) and sets it executable with `chmod +x`."
        },
        {
          "id": "US-001-AC06",
          "text": "If `~/.local/bin` is not on `$PATH`, the script prints the exact export line to add to `~/.bashrc`, `~/.zshrc`, or `~/.profile`, and instructs the user to open a new terminal."
        },
        {
          "id": "US-001-AC07",
          "text": "On success, the script prints \"parallax X.X.X installed. Run: parallax install\" to guide the next step."
        },
        {
          "id": "US-001-AC08",
          "text": "Re-running the script when a binary already exists prints \"Updating parallax from vX.X.X to vY.Y.Y.\" and replaces the binary."
        },
        {
          "id": "US-001-AC09",
          "text": "If the GitHub API request fails, the script prints \"Could not fetch latest release. Set PARALLAX_VERSION=vX.X.X to install a specific version.\" and exits with code 1."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Install parallax on Windows with one command",
      "description": "As a non-developer on Windows, I want to paste a single PowerShell command so that `parallax` is available in my terminal without downloading or configuring anything manually.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "The `install.ps1` script always downloads `parallax-windows-x86_64.exe` (only x86_64 supported in this iteration)."
        },
        {
          "id": "US-002-AC02",
          "text": "The script queries the GitHub Releases API to obtain the latest tag; if `$env:PARALLAX_VERSION` is set, it uses that version instead."
        },
        {
          "id": "US-002-AC03",
          "text": "The script downloads the binary and its `.sha256` file using `Invoke-WebRequest` with a visible progress bar."
        },
        {
          "id": "US-002-AC04",
          "text": "The script verifies the checksum using `Get-FileHash -Algorithm SHA256`; if it does not match, it deletes the file, prints \"Checksum verification failed. Aborting.\", and exits with code 1."
        },
        {
          "id": "US-002-AC05",
          "text": "The script installs the binary to `$env:APPDATA\\parallax\\bin\\parallax.exe`, creating the directory if needed."
        },
        {
          "id": "US-002-AC06",
          "text": "The script adds `$env:APPDATA\\parallax\\bin` to the user's `PATH` via `[Environment]::SetEnvironmentVariable(..., \"User\")` if not already present."
        },
        {
          "id": "US-002-AC07",
          "text": "On success, the script prints \"parallax X.X.X installed. Open a new terminal and run: parallax install\"."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "`install.sh` is written in POSIX `sh` (no bash-isms: no `[[`, no `$BASH_VERSION`, no `source`) so it runs on Alpine Linux, Debian, and macOS without modification."
    },
    {
      "id": "FR-2",
      "description": "`install.ps1` requires PowerShell 5.1+ (ships with Windows 10) and uses no external tools."
    },
    {
      "id": "FR-3",
      "description": "Both scripts define the GitHub repo as a single variable at the top (`REPO=\"quinteroac/comfy-diffusion\"` / `$Repo = \"quinteroac/comfy-diffusion\"`) to simplify future rebranding."
    },
    {
      "id": "FR-4",
      "description": "Both scripts accept `PARALLAX_INSTALL_DIR` / `$env:PARALLAX_INSTALL_DIR` as an override for the install path."
    },
    {
      "id": "FR-5",
      "description": "Neither script requires elevated privileges — all paths are user-owned (`~/.local/bin`, `$APPDATA`)."
    },
    {
      "id": "FR-6",
      "description": "The GitHub Releases API is queried at `https://api.github.com/repos/{REPO}/releases/latest`; the tag is extracted from the `tag_name` field."
    },
    {
      "id": "FR-7",
      "description": "The README is updated to show the one-liner install commands for Linux/macOS and Windows using the raw GitHub URLs."
    }
  ]
}
