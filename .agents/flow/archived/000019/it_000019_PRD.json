{
  "goals": [
    "Allow callers to load any multi-file text encoder (SDXL, SD3, Flux, HunyuanVideo, etc.) with a single `load_clip()` call.",
    "Preserve full backward compatibility: every existing single-path call site continues to work unchanged.",
    "No new package dependencies; all tests pass in a CPU-only environment."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Variadic path acceptance in `load_clip`",
      "description": "As a library consumer, I want to call `manager.load_clip(path1, path2, clip_type=\"sdxl\")` so that I can load dual-encoder models (e.g. SDXL, Flux) without resorting to `load_ltxav_text_encoder` or manual ComfyUI internals.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`ModelManager.load_clip` signature is `load_clip(self, *paths: str | Path, clip_type: str = \"stable_diffusion\") -> Any`."
        },
        {
          "id": "US-001-AC02",
          "text": "Calling with a single path (e.g. `manager.load_clip(\"clip_l.safetensors\")`) continues to work identically to the previous behaviour."
        },
        {
          "id": "US-001-AC03",
          "text": "Calling with two paths (e.g. `manager.load_clip(\"clip_l.safetensors\", \"t5xxl_fp16.safetensors\", clip_type=\"flux\")`) resolves both paths and passes them as `ckpt_paths=[path1, path2]` to `comfy.sd.load_clip`."
        },
        {
          "id": "US-001-AC04",
          "text": "Each individual path is resolved independently using the same rule as before: absolute paths are validated to be existing files; relative names are looked up via `folder_paths.get_full_path_or_raise(\"text_encoders\", name)`."
        },
        {
          "id": "US-001-AC05",
          "text": "If any one path does not resolve to an existing file, a `FileNotFoundError` is raised before the ComfyUI loader is called, with the message `\"clip file not found: <path>\"`."
        },
        {
          "id": "US-001-AC06",
          "text": "Calling with zero paths raises a `ValueError` with message `\"load_clip requires at least one path\"`."
        },
        {
          "id": "US-001-AC07",
          "text": "An unrecognised `clip_type` string raises `ValueError` with a message containing the invalid value and listing all valid type names (lowercased)."
        },
        {
          "id": "US-001-AC08",
          "text": "Type annotations on the public API are updated to reflect the variadic signature."
        },
        {
          "id": "US-001-AC09",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Backward-compatible test suite",
      "description": "As a maintainer, I want the existing `test_model_manager_clip_loading.py` tests to continue passing so that no regression is introduced in the single-path code path.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "All existing tests in `tests/test_model_manager_clip_loading.py` pass without modification to the test file."
        },
        {
          "id": "US-002-AC02",
          "text": "A new test covers the two-path scenario: two files created, both resolved to absolute paths, `comfy.sd.load_clip` receives `ckpt_paths` with both resolved strings."
        },
        {
          "id": "US-002-AC03",
          "text": "A new test covers the zero-path scenario: `load_clip()` with no arguments raises `ValueError`."
        },
        {
          "id": "US-002-AC04",
          "text": "A new test covers the partial-missing-file scenario: first path exists, second path does not → `FileNotFoundError` raised before the loader is called."
        },
        {
          "id": "US-002-AC05",
          "text": "`uv run pytest` passes in a CPU-only environment."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "`ModelManager.load_clip` MUST accept one or more positional `str | Path` arguments via `*paths`."
    },
    {
      "id": "FR-2",
      "description": "Zero positional arguments MUST raise `ValueError(\"load_clip requires at least one path\")`."
    },
    {
      "id": "FR-3",
      "description": "Each path in `*paths` MUST be resolved independently: absolute existing files are used as-is; relative names are resolved via `folder_paths.get_full_path_or_raise(\"text_encoders\", name)`. An absolute path that does not point to an existing file MUST raise `FileNotFoundError`."
    },
    {
      "id": "FR-4",
      "description": "All resolved paths are forwarded to `comfy.sd.load_clip(ckpt_paths=[...], embedding_directory=..., clip_type=...)` as a list in the same order they were provided."
    },
    {
      "id": "FR-5",
      "description": "`clip_type` is a keyword-only `str` argument with default `\"stable_diffusion\"`. It is mapped to `comfy_sd.CLIPType` by looking up `clip_type.upper()` as an enum member name. If the name does not match any member, a `ValueError` MUST be raised with a message that includes the invalid value and lists all valid `CLIPType` member names (lowercased)."
    },
    {
      "id": "FR-6",
      "description": "The lazy-import pattern is preserved — no `torch`, `comfy.*`, or `folder_paths` imports at module top level."
    },
    {
      "id": "FR-7",
      "description": "`load_ltxav_text_encoder` is left untouched (backward compatibility); no deprecation warning is added in this iteration."
    }
  ]
}
