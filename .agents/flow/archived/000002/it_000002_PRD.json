{
  "goals": [
    "Expose a minimal, ergonomic API (`ModelManager` + `CheckpointResult`) for loading checkpoints.",
    "Delegate all parsing and tensor work to `comfy.sd.load_checkpoint_guess_config()` — pycomfy",
    "Guarantee that `from pycomfy.models import ModelManager` never crashes on a CPU-only",
    "Surface actionable Python exceptions (not raw comfy tracebacks) for the two most common failure",
    "Pass `ruff check .` and `mypy pycomfy/` without regressions."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Import ModelManager with no side effects",
      "description": "As a Python developer, I want to import `ModelManager` from `pycomfy.models` so that I can set up the manager before any model file is touched, without triggering unexpected errors or GPU initialisation.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`from pycomfy.models import ModelManager` succeeds on a CPU-only machine with no model files"
        },
        {
          "id": "US-001-AC02",
          "text": "The import does not load any model files, allocate GPU memory, or modify global state beyond"
        },
        {
          "id": "US-001-AC03",
          "text": "`ruff check .` passes (no lint errors introduced by the new module)."
        },
        {
          "id": "US-001-AC04",
          "text": "`mypy pycomfy/` passes in strict mode (no type errors introduced by the new module)."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Instantiate ModelManager with a valid models directory",
      "description": "As a Python developer, I want to construct `ModelManager(models_dir=\"/path/to/models\")` so that the manager knows where to look for checkpoint files without me having to pass the path on every load call.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "`ModelManager(models_dir=<existing_dir>)` constructs successfully and silently."
        },
        {
          "id": "US-002-AC02",
          "text": "`ModelManager(models_dir=<non_existing_dir>)` raises `ValueError` with a message that"
        },
        {
          "id": "US-002-AC03",
          "text": "The raised exception is a `ValueError`, never a raw exception propagated from comfy internals."
        },
        {
          "id": "US-002-AC04",
          "text": "`models_dir` is stored as a `pathlib.Path` internally; the constructor accepts both `str`"
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Load a checkpoint and receive a typed CheckpointResult",
      "description": "As a Python developer (or a future pycomfy module), I want to call `manager.load_checkpoint(\"animagine-xl.safetensors\")` so that I receive a `CheckpointResult` with `.model`, `.clip`, and `.vae` attributes that are the raw comfy objects — ready to pass directly to sampling and conditioning APIs.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "`load_checkpoint(filename)` accepts a filename (not a full path); the full path is resolved"
        },
        {
          "id": "US-003-AC02",
          "text": "Returns a `CheckpointResult` dataclass (or equivalent typed container) with three"
        },
        {
          "id": "US-003-AC03",
          "text": "The three attributes hold the native objects returned by"
        },
        {
          "id": "US-003-AC04",
          "text": "`.clip` and `.vae` are typed `Any | None`; the loader does not raise when comfy returns"
        },
        {
          "id": "US-003-AC05",
          "text": "`CheckpointResult` is importable directly: `from pycomfy.models import CheckpointResult`."
        },
        {
          "id": "US-003-AC06",
          "text": "`mypy` accepts the type annotations (comfy internals typed as `Any` via"
        }
      ]
    },
    {
      "id": "US-004",
      "title": "Graceful error when the checkpoint file is missing",
      "description": "As a Python developer, I want `load_checkpoint` to raise a clear `FileNotFoundError` (not a raw comfy traceback) when the requested file does not exist so that I can quickly identify and fix the issue.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "`manager.load_checkpoint(\"nonexistent.safetensors\")` raises `FileNotFoundError`."
        },
        {
          "id": "US-004-AC02",
          "text": "The exception message includes the full resolved path that was looked up."
        },
        {
          "id": "US-004-AC03",
          "text": "No partial comfy stack trace leaks through — the exception is raised by pycomfy before"
        },
        {
          "id": "US-004-AC04",
          "text": "A pytest test covers this path and passes on CPU without a GPU."
        }
      ]
    },
    {
      "id": "US-005",
      "title": "CPU-only smoke test — import never crashes without a GPU",
      "description": "As a CI pipeline, I want `from pycomfy.models import ModelManager` to succeed on a CPU-only machine so that automated tests remain green without GPU hardware.",
      "acceptanceCriteria": [
        {
          "id": "US-005-AC01",
          "text": "`uv run python -c \"from pycomfy.models import ModelManager; print('ok')\"` exits with code 0"
        },
        {
          "id": "US-005-AC02",
          "text": "A pytest test asserts that `ModelManager` is importable and can be instantiated with a valid"
        },
        {
          "id": "US-005-AC03",
          "text": "The test does NOT attempt to call `load_checkpoint()` with a real model file (that would"
        }
      ]
    }
  ],
  "functionalRequirements": []
}
