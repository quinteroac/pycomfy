{
  "goals": [
    "Allow developers to encode a PIL image into a latent tensor (enabling img2img pipelines).",
    "Allow developers to load VAE, CLIP, and UNet objects independently from separate checkpoint files.",
    "Maintain CPU-safe import behaviour — no torch/comfy side effects at module import time.",
    "Keep `vae_encode` accessible from both `pycomfy.vae` and `pycomfy` (top-level re-export), matching the pattern established by `vae_decode`."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Encode PIL image to latent",
      "description": "As a Python developer (or internal pycomfy module), I want to call `vae_encode(vae, image)` with a PIL `Image` and receive a `{\"samples\": tensor}` dict so that I can feed the result directly into `sample()` for img2img inference.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`vae_encode(vae, image)` accepts a `PIL.Image.Image` and a VAE object; returns `dict` with key `\"samples\"` whose value is a tensor."
        },
        {
          "id": "US-001-AC02",
          "text": "The returned dict format is identical to the `latent` dict consumed by `sample()`."
        },
        {
          "id": "US-001-AC03",
          "text": "The function is defined in `pycomfy/vae.py` and added to `__all__`."
        },
        {
          "id": "US-001-AC04",
          "text": "`from pycomfy.vae import vae_encode` works on CPU-only with no torch/comfy import at module level."
        },
        {
          "id": "US-001-AC05",
          "text": "`from pycomfy import vae_encode` works (re-exported from `__init__.py`)."
        },
        {
          "id": "US-001-AC06",
          "text": "Unit test (mock VAE): `vae_encode(mock_vae, pil_image)` returns `{\"samples\": mock_tensor}`."
        },
        {
          "id": "US-001-AC07",
          "text": "Typecheck / lint passes (`ruff check . && mypy pycomfy/` no new violations)."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Round-trip VAE smoke test",
      "description": "As a developer, I want `vae_decode(vae, vae_encode(vae, image))` to return a `PIL.Image.Image` without errors so that I can verify the encode→decode pipeline is coherent end-to-end on CPU.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "A test exercises `vae_decode(mock_vae, vae_encode(mock_vae, pil_image))` on CPU (mocked VAE)."
        },
        {
          "id": "US-002-AC02",
          "text": "The test completes without raising; the return value is an instance of `PIL.Image.Image`."
        },
        {
          "id": "US-002-AC03",
          "text": "`uv run pytest` passes on CPU-only CI."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Load standalone VAE",
      "description": "As a Python developer, I want to call `manager.load_vae(path)` so that I can obtain a VAE object usable by both `vae_encode` and `vae_decode` without loading a full checkpoint.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "`ModelManager.load_vae(path: str | Path)` is implemented and added to `ModelManager`."
        },
        {
          "id": "US-003-AC02",
          "text": "The method calls the appropriate ComfyUI internal loader (grep-verified against `comfy/sd.py` before implementation)."
        },
        {
          "id": "US-003-AC03",
          "text": "Returns the raw comfy VAE object."
        },
        {
          "id": "US-003-AC04",
          "text": "Raises `FileNotFoundError` if `path` does not point to an existing file."
        },
        {
          "id": "US-003-AC05",
          "text": "Unit test (mock): confirms the correct comfy internal is called with the resolved path."
        },
        {
          "id": "US-003-AC06",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "Load standalone CLIP",
      "description": "As a Python developer, I want to call `manager.load_clip(path)` so that I can obtain a CLIP object usable by `encode_prompt` when not loading a full checkpoint.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "`ModelManager.load_clip(path: str | Path)` is implemented and added to `ModelManager`."
        },
        {
          "id": "US-004-AC02",
          "text": "The method calls the appropriate ComfyUI internal loader (grep-verified against `comfy/sd.py` before implementation)."
        },
        {
          "id": "US-004-AC03",
          "text": "Returns the raw comfy CLIP object."
        },
        {
          "id": "US-004-AC04",
          "text": "Raises `FileNotFoundError` if `path` does not point to an existing file."
        },
        {
          "id": "US-004-AC05",
          "text": "Unit test (mock): confirms the correct comfy internal is called with the resolved path."
        },
        {
          "id": "US-004-AC06",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-005",
      "title": "Load standalone UNet",
      "description": "As a Python developer, I want to call `manager.load_unet(path)` so that I can obtain a model/UNet object usable by `sample()` without loading a full checkpoint.",
      "acceptanceCriteria": [
        {
          "id": "US-005-AC01",
          "text": "`ModelManager.load_unet(path: str | Path)` is implemented and added to `ModelManager`."
        },
        {
          "id": "US-005-AC02",
          "text": "The method calls `comfy.sd.load_diffusion_model(str(path))` directly — **not** the deprecated `comfy.sd.load_unet` (which emits a deprecation warning)."
        },
        {
          "id": "US-005-AC03",
          "text": "Returns the raw comfy model object."
        },
        {
          "id": "US-005-AC04",
          "text": "Raises `FileNotFoundError` if `path` does not point to an existing file."
        },
        {
          "id": "US-005-AC05",
          "text": "Unit test (mock): confirms the correct comfy internal is called with the resolved path."
        },
        {
          "id": "US-005-AC06",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-006",
      "title": "No regression on existing ModelManager",
      "description": "As a developer, I want `ModelManager` and `load_checkpoint` to continue working exactly as before so that existing code built on it_002–006 is not broken.",
      "acceptanceCriteria": [
        {
          "id": "US-006-AC01",
          "text": "All pre-existing tests for `ModelManager.load_checkpoint` still pass."
        },
        {
          "id": "US-006-AC02",
          "text": "`from pycomfy.models import ModelManager` works without error on CPU-only."
        },
        {
          "id": "US-006-AC03",
          "text": "`uv run pytest` passes entirely."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "** `vae_encode(vae, image: PIL.Image.Image) -> dict[str, Any]` — defined in `pycomfy/vae.py`, lazily imports torch/comfy inside the function body."
    },
    {
      "id": "FR-2",
      "description": "** `vae_encode` converts the PIL image to a normalised float tensor `[0, 1]` in NHWC format before calling `vae.encode(...)`, matching ComfyUI's `VAEEncode` node internals (grep `VAEEncode` in `vendor/ComfyUI/nodes.py` for reference)."
    },
    {
      "id": "FR-3",
      "description": "** The returned dict has the shape `{\"samples\": tensor}` where tensor is the output of `vae.encode(pixels)` — same schema as the latent dicts produced by `sample()`."
    },
    {
      "id": "FR-4",
      "description": "** `vae_encode` is added to `pycomfy/__init__.py` re-exports and `pycomfy/vae.py` `__all__`, following the same pattern as `vae_decode`."
    },
    {
      "id": "FR-5",
      "description": "** `ModelManager.load_vae(path: str | Path) -> Any` — calls the appropriate `comfy.sd` loader; raises `FileNotFoundError` on missing file; lazy comfy import inside the method."
    },
    {
      "id": "FR-6",
      "description": "** `ModelManager.load_clip(path: str | Path) -> Any` — calls the appropriate `comfy.sd` loader; raises `FileNotFoundError` on missing file; lazy comfy import inside the method."
    },
    {
      "id": "FR-7",
      "description": "** `ModelManager.load_unet(path: str | Path) -> Any` — calls `comfy.sd.load_diffusion_model(str(path))` directly (NOT `comfy.sd.load_unet`, which is deprecated and emits a warning); raises `FileNotFoundError` on missing file; lazy comfy import inside the method."
    },
    {
      "id": "FR-8",
      "description": "** All three standalone loaders accept `str | Path`; the path is resolved to absolute before passing to ComfyUI internals."
    },
    {
      "id": "FR-9",
      "description": "** No `torch`, `comfy.*`, or `ensure_comfyui_on_path()` calls at module top level in `vae.py` or `models.py` — all deferred to function/method body call time."
    },
    {
      "id": "FR-10",
      "description": "** Before implementing, grep `VAEEncode`, `load_vae`, `load_clip`, `load_unet` in `vendor/ComfyUI/nodes.py` and `vendor/ComfyUI/comfy/sd.py` to identify the exact internal APIs to call."
    }
  ]
}
