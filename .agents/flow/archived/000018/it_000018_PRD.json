{
  "goals": [
    "Make `comfy-diffusion` installable via `pip install .` or `uv pip install .` with correct metadata and dependency resolution",
    "Provide functional extras groups (`[cuda]`, `[cpu]`, `[video]`, `[audio]`, `[all]`) so consumers pick only what they need",
    "Enable IDE autocompletion and type checking for the public API via `py.typed` marker and type stubs",
    "Bundle agent skills as package data in `comfy_diffusion/skills/` so they ship in the wheel and are discoverable at runtime via `importlib.resources`"
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Base Installation",
      "description": "As a Python developer, I want to install `comfy-diffusion` from the repo root with `pip install .` or `uv pip install .` so that I can use it as a dependency in my own project.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`pyproject.toml` has complete PEP 621 metadata (name, version, description, license, requires-python >= 3.12, authors, URLs)"
        },
        {
          "id": "US-001-AC02",
          "text": "Core dependencies are declared (excluding `torch`, which is optional-only)"
        },
        {
          "id": "US-001-AC03",
          "text": "`uv pip install .` in a clean venv succeeds without errors"
        },
        {
          "id": "US-001-AC04",
          "text": "`import comfy_diffusion` works after install"
        },
        {
          "id": "US-001-AC05",
          "text": "`comfy_diffusion.check_runtime()` returns a valid diagnostics dict after install"
        },
        {
          "id": "US-001-AC06",
          "text": "`torch` is NOT pulled in by a base install"
        },
        {
          "id": "US-001-AC07",
          "text": "ComfyUI submodule is NOT declared as a pip dependency — it remains vendored"
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Extras Groups",
      "description": "As a Python developer, I want to install specific extras like `comfy-diffusion[cuda]` or `comfy-diffusion[all]` so that I only install the heavy dependencies I actually need.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "`[cuda]` extra installs torch with CUDA support"
        },
        {
          "id": "US-002-AC02",
          "text": "`[cpu]` extra installs torch CPU-only"
        },
        {
          "id": "US-002-AC03",
          "text": "`[video]` extra installs `opencv-python` and `imageio`"
        },
        {
          "id": "US-002-AC04",
          "text": "`[audio]` extra installs `torchaudio`"
        },
        {
          "id": "US-002-AC05",
          "text": "`[all]` extra installs the union of all above extras"
        },
        {
          "id": "US-002-AC06",
          "text": "Installing an extra does not break the base install"
        },
        {
          "id": "US-002-AC07",
          "text": "Typecheck / lint passes"
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Type Stubs and py.typed",
      "description": "As a Python developer using an IDE, I want type information for `comfy-diffusion`'s public API so that I get autocompletion and type checking without additional configuration.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "`comfy_diffusion/py.typed` marker file exists and is included in the wheel"
        },
        {
          "id": "US-003-AC02",
          "text": "Public API functions have inline type annotations (parameters and return types)"
        },
        {
          "id": "US-003-AC03",
          "text": "Pylance / pyright resolves types for public symbols (`check_runtime`, `vae_decode`, `vae_encode`, `apply_lora`, `encode_prompt`, `sample`, etc.)"
        },
        {
          "id": "US-003-AC04",
          "text": "`py.typed` is declared in `pyproject.toml` package data so it ships in the wheel"
        },
        {
          "id": "US-003-AC05",
          "text": "Typecheck / lint passes"
        }
      ]
    },
    {
      "id": "US-004",
      "title": "Distributable Agent Skills",
      "description": "As a consumer of the library (human or AI agent), I want agent skills bundled in `comfy_diffusion/skills/` so that I can discover and load them at runtime from an installed package.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "`comfy_diffusion/skills/` directory exists with skill files (`.md` or other formats)"
        },
        {
          "id": "US-004-AC02",
          "text": "Skills are declared as `package_data` (or equivalent) in `pyproject.toml` so they are included in the built wheel"
        },
        {
          "id": "US-004-AC03",
          "text": "Skills are discoverable at runtime via `importlib.resources` (e.g., `importlib.resources.files(\"comfy_diffusion.skills\")`)"
        },
        {
          "id": "US-004-AC04",
          "text": "Skills in `comfy_diffusion/skills/` are distinct from `.agents/skills/` (internal dev workflow) — no confusion between the two"
        },
        {
          "id": "US-004-AC05",
          "text": "Typecheck / lint passes"
        }
      ]
    },
    {
      "id": "US-005",
      "title": "Smoke Test Post-Install",
      "description": "As a developer, I want a smoke test that validates the package installs and imports correctly so that regressions in packaging are caught early.",
      "acceptanceCriteria": [
        {
          "id": "US-005-AC01",
          "text": "A test (or script) exists that: installs `comfy-diffusion` in a clean venv, imports it, and calls `check_runtime()`"
        },
        {
          "id": "US-005-AC02",
          "text": "The test verifies that `comfy_diffusion/skills/` data files are accessible via `importlib.resources`"
        },
        {
          "id": "US-005-AC03",
          "text": "The test verifies that `py.typed` is present in the installed package"
        },
        {
          "id": "US-005-AC04",
          "text": "The test can run in CI (CPU-only, no GPU required)"
        },
        {
          "id": "US-005-AC05",
          "text": "Typecheck / lint passes"
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "`pyproject.toml` must contain complete PEP 621 metadata with `requires-python = \">=3.12\"`"
    },
    {
      "id": "FR-2",
      "description": "`torch` must never appear in core `dependencies` — only in `[cuda]` and `[cpu]` optional-dependencies"
    },
    {
      "id": "FR-3",
      "description": "ComfyUI vendored submodule must not be declared as a package dependency"
    },
    {
      "id": "FR-4",
      "description": "`py.typed` marker file must be included in `package_data` so it ships in wheels and sdists"
    },
    {
      "id": "FR-5",
      "description": "`comfy_diffusion/skills/*.md` (and any other skill files) must be declared in `package_data` so they ship in the wheel"
    },
    {
      "id": "FR-6",
      "description": "A runtime utility must allow discovering bundled skills via `importlib.resources.files(\"comfy_diffusion.skills\")`"
    },
    {
      "id": "FR-7",
      "description": "All existing tests must continue to pass after `pyproject.toml` changes"
    },
    {
      "id": "FR-8",
      "description": "The package must be installable with both `pip install .` and `uv pip install .`"
    }
  ]
}
