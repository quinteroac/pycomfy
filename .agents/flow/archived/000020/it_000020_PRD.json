{
  "goals": [
    "Expose `generate_text()` and `generate_ltx2_prompt()` as importable functions in `comfy_diffusion.textgen`.",
    "Extend `ModelManager` with a `load_llm()` method that follows the existing loader pattern (`load_clip`, `load_vae`, etc.).",
    "All functions must be testable on CPU-only environments (mocked LLM calls in tests)."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Load an LLM model via `ModelManager.load_llm()`",
      "description": "As a developer, I want to load an LLM/VLM model through `ModelManager.load_llm(path)` so that I can obtain a model object to pass to text-generation functions without manually managing `folder_paths` or ComfyUI internals.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`ModelManager` gains a `load_llm(path: str | Path) -> Any` method."
        },
        {
          "id": "US-001-AC02",
          "text": "If `path` is an absolute path pointing to an existing file, that file is loaded directly."
        },
        {
          "id": "US-001-AC03",
          "text": "If `path` is a relative name, it is resolved under the `llm` subfolder of `models_dir` (e.g. `models_dir/llm/<name>`)."
        },
        {
          "id": "US-001-AC04",
          "text": "If the file does not exist, `FileNotFoundError` is raised with a descriptive message."
        },
        {
          "id": "US-001-AC05",
          "text": "`folder_paths` registers `models_dir/llm` as the `llm` model folder during `ModelManager.__init__`."
        },
        {
          "id": "US-001-AC06",
          "text": "No `torch` or `comfy.*` imports at module top level — all deferred to call time."
        },
        {
          "id": "US-001-AC07",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Generate text with `generate_text()`",
      "description": "As a developer, I want to call `generate_text(clip, prompt, ...)` so that I can run LLM/VLM inference and receive the generated string directly in my Python script.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "`comfy_diffusion.textgen` module exists and exports `generate_text`."
        },
        {
          "id": "US-002-AC02",
          "text": "Signature: `generate_text(clip: Any, prompt: str, *, image: Any | None = None, max_length: int = 256, do_sample: bool = True, temperature: float = 0.7, top_k: int = 64, top_p: float = 0.95, min_p: float = 0.05, repetition_penalty: float = 1.05, seed: int = 0) -> str`."
        },
        {
          "id": "US-002-AC03",
          "text": "Internally calls `clip.tokenize()`, `clip.generate()`, and `clip.decode()` — mirroring `TextGenerate.execute()` without depending on ComfyUI's node wiring."
        },
        {
          "id": "US-002-AC04",
          "text": "Returns the decoded string (no trailing whitespace beyond what `clip.decode` produces)."
        },
        {
          "id": "US-002-AC05",
          "text": "When `do_sample=False`, sampling parameters (`temperature`, `top_k`, `top_p`, `min_p`, `repetition_penalty`, `seed`) are not forwarded (matches node `sampling_mode=\"off\"` behaviour)."
        },
        {
          "id": "US-002-AC06",
          "text": "No `torch` or `comfy.*` imports at module top level."
        },
        {
          "id": "US-002-AC07",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Generate an LTX-Video 2 enhanced prompt with `generate_ltx2_prompt()`",
      "description": "As a developer, I want to call `generate_ltx2_prompt(clip, prompt, ...)` so that I can refine a raw user prompt into a detailed video-generation prompt suitable for LTX-Video 2, using the LTX2 system prompt template automatically.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "`comfy_diffusion.textgen` exports `generate_ltx2_prompt`."
        },
        {
          "id": "US-003-AC02",
          "text": "Signature matches `generate_text` exactly (same parameters, same return type)."
        },
        {
          "id": "US-003-AC03",
          "text": "When `image=None`, the T2V system prompt template is prepended to `prompt` before calling `generate_text` (mirrors `TextGenerateLTX2Prompt.execute()` — `LTX2_T2V_SYSTEM_PROMPT`)."
        },
        {
          "id": "US-003-AC04",
          "text": "When `image` is provided, the I2V system prompt template is prepended (mirrors `LTX2_I2V_SYSTEM_PROMPT`)."
        },
        {
          "id": "US-003-AC05",
          "text": "The system prompt templates are copied verbatim from `comfy_extras/nodes_textgen.py` (do not paraphrase or truncate)."
        },
        {
          "id": "US-003-AC06",
          "text": "Typecheck / lint passes."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "`comfy_diffusion/textgen.py` is the implementation file for `generate_text` and `generate_ltx2_prompt`."
    },
    {
      "id": "FR-2",
      "description": "`ModelManager.load_llm(path: str | Path) -> Any` loads an LLM via the ComfyUI CLIP loader with the appropriate type for LLM models."
    },
    {
      "id": "FR-3",
      "description": "`ModelManager.__init__` registers `models_dir/llm` in `folder_paths` under the `\"llm\"` key."
    },
    {
      "id": "FR-4",
      "description": "`comfy_diffusion/__init__.py` does **not** re-export `generate_text` or `generate_ltx2_prompt` at package level — callers must use `from comfy_diffusion.textgen import generate_text` (consistent with non-convenience-exported modules)."
    },
    {
      "id": "FR-5",
      "description": "All lazy-import rules apply: no `torch` or `comfy.*` at module top level in `textgen.py`."
    },
    {
      "id": "FR-6",
      "description": "Tests live in `tests/test_textgen.py` and must pass on CPU-only CI (use mocks/stubs for `clip` object — no real model weights required)."
    }
  ]
}
