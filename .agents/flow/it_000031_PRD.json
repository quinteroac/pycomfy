{
  "goals": [
    "Expose `load_audio_encoder()` in `ModelManager` so callers can load an audio encoder without",
    "Expose `audio_encoder_encode()` in `comfy_diffusion.audio` so callers can encode audio",
    "Expose `wan_sound_image_to_video()` and `wan_sound_image_to_video_extend()` in",
    "Expose `wan22_image_to_video_latent()` in `comfy_diffusion.latent` for TI2V latent creation.",
    "Implement five WAN 2.2 pipelines under `comfy_diffusion/pipelines/video/wan/wan22/`:",
    "Provide one example script per pipeline under `examples/`.",
    "Wire the new `wan22` sub-package into `comfy_diffusion/pipelines/video/wan/__init__.py`."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "`load_audio_encoder` in ModelManager",
      "description": "As a Python developer, I want `ModelManager.load_audio_encoder(path)` so that I can load a wav2vec2 (or compatible) audio encoder without calling ComfyUI internals directly.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`ModelManager.load_audio_encoder(path: str | Path) -> Any` is implemented in `models.py`."
        },
        {
          "id": "US-001-AC02",
          "text": "The method registers `audio_encoders` as a known models directory (analogous to how"
        },
        {
          "id": "US-001-AC03",
          "text": "Internally it calls `comfy.utils.load_torch_file` and"
        },
        {
          "id": "US-001-AC04",
          "text": "The method follows the lazy-import pattern (no `torch` or `comfy.*` at module top level)."
        },
        {
          "id": "US-001-AC05",
          "text": "`load_audio_encoder` is listed in `ModelManager`'s public interface (docstring or type stub)."
        },
        {
          "id": "US-001-AC06",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "`audio_encoder_encode` in `audio.py`",
      "description": "As a Python developer, I want `audio_encoder_encode(audio_encoder, audio)` in `comfy_diffusion.audio` so that I can encode a loaded audio dict into an `AUDIO_ENCODER_OUTPUT` object suitable for S2V conditioning.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "`audio_encoder_encode(audio_encoder: Any, audio: dict) -> Any` is implemented in `audio.py`."
        },
        {
          "id": "US-002-AC02",
          "text": "The function calls `audio_encoder.encode_audio(audio[\"waveform\"], audio[\"sample_rate\"])`,"
        },
        {
          "id": "US-002-AC03",
          "text": "The function follows the lazy-import pattern (no `torch` or `comfy.*` at module top level)."
        },
        {
          "id": "US-002-AC04",
          "text": "`\"audio_encoder_encode\"` is added to `audio.__all__`."
        },
        {
          "id": "US-002-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "`wan_sound_image_to_video` and `wan_sound_image_to_video_extend` in `conditioning.py`",
      "description": "As a Python developer, I want S2V conditioning helpers in `comfy_diffusion.conditioning` so that I can build audio-driven video conditioning without calling raw ComfyUI nodes.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "`wan_sound_image_to_video(positive, negative, vae, width, height, length, batch_size, *, audio_encoder_output=None, ref_image=None, control_video=None, ref_motion=None)` is implemented, mirrors `WanSoundImageToVideo.execute()`, returns `(positive, negative, latent)`."
        },
        {
          "id": "US-003-AC02",
          "text": "`wan_sound_image_to_video_extend(positive, negative, vae, length, video_latent, *, audio_encoder_output=None, ref_image=None, control_video=None)` is implemented, mirrors `WanSoundImageToVideoExtend.execute()`, returns `(positive, negative, latent)`."
        },
        {
          "id": "US-003-AC03",
          "text": "Both functions follow the lazy-import pattern."
        },
        {
          "id": "US-003-AC04",
          "text": "Both names are added to `conditioning.__all__`."
        },
        {
          "id": "US-003-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "`wan22_image_to_video_latent` in `latent.py`",
      "description": "As a Python developer, I want `wan22_image_to_video_latent(vae, width, height, length, batch_size, *, start_image=None)` in `comfy_diffusion.latent` so that I can create TI2V latents for WAN 2.2 without calling ComfyUI nodes directly.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "`wan22_image_to_video_latent(vae: Any, width: int, height: int, length: int = 49, batch_size: int = 1, *, start_image: Any = None) -> dict[str, Any]` is implemented in `latent.py`."
        },
        {
          "id": "US-004-AC02",
          "text": "The returned dict has `\"samples\"` (shape `[batch, 48, ((length-1)//4)+1, height//16, width//16]`) processed through `comfy.latent_formats.Wan22()`, and `\"noise_mask\"` when `start_image` is provided — matching `Wan22ImageToVideoLatent.execute()`."
        },
        {
          "id": "US-004-AC03",
          "text": "The function follows the lazy-import pattern."
        },
        {
          "id": "US-004-AC04",
          "text": "`\"wan22_image_to_video_latent\"` is added to `latent.__all__`."
        },
        {
          "id": "US-004-AC05",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-005",
      "title": "WAN 2.2 text-to-video pipeline (`t2v`)",
      "description": "As a Python developer, I want `comfy_diffusion.pipelines.video.wan.wan22.t2v` with `manifest()` and `run()` so that I can generate video from a text prompt using the WAN 2.2 T2V 14B model.",
      "acceptanceCriteria": [
        {
          "id": "US-005-AC01",
          "text": "`manifest()` returns exactly 6 `ModelEntry` items matching the active nodes in"
        },
        {
          "id": "US-005-AC02",
          "text": "`run(prompt, negative_prompt, width, height, length, *, models_dir, seed, steps, cfg) -> list[PIL.Image]` executes the dual two-pass `KSamplerAdvanced` flow (high-noise pass first, then low-noise pass) with `LoraLoaderModelOnly` applied to each UNet and `ModelSamplingSD3` shift applied, exactly as ordered in the workflow."
        },
        {
          "id": "US-005-AC03",
          "text": "BYPASSED nodes are not included in `manifest()` or `run()`."
        },
        {
          "id": "US-005-AC04",
          "text": "Returns a list of `PIL.Image` frames decoded via `vae_decode`."
        },
        {
          "id": "US-005-AC05",
          "text": "A unit test verifies `manifest()` length and field names with mocked loaders."
        },
        {
          "id": "US-005-AC06",
          "text": "An example script `examples/wan22_t2v.py` demonstrates usage."
        },
        {
          "id": "US-005-AC07",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-006",
      "title": "WAN 2.2 image-to-video pipeline (`i2v`)",
      "description": "As a Python developer, I want `comfy_diffusion.pipelines.video.wan.wan22.i2v` with `manifest()` and `run()` so that I can animate an input image using the WAN 2.2 I2V 14B model.",
      "acceptanceCriteria": [
        {
          "id": "US-006-AC01",
          "text": "`manifest()` returns exactly 6 `ModelEntry` items matching active nodes in"
        },
        {
          "id": "US-006-AC02",
          "text": "`run(image, prompt, negative_prompt, width, height, length, *, models_dir, seed, steps, cfg) -> list[PIL.Image]` executes `WanImageToVideo` conditioning followed by two-pass `KSamplerAdvanced` with `ModelSamplingSD3` and the four `ComfySwitchNode` switches resolved at their default values (`False` = no LoRA branch active), matching the workflow's default configuration."
        },
        {
          "id": "US-006-AC03",
          "text": "BYPASSED nodes are not included."
        },
        {
          "id": "US-006-AC04",
          "text": "A unit test verifies `manifest()` length and field names."
        },
        {
          "id": "US-006-AC05",
          "text": "An example script `examples/wan22_i2v.py` demonstrates usage."
        },
        {
          "id": "US-006-AC06",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-007",
      "title": "WAN 2.2 first-last-frame-to-video pipeline (`flf2v`)",
      "description": "As a Python developer, I want `comfy_diffusion.pipelines.video.wan.wan22.flf2v` with `manifest()` and `run()` so that I can interpolate between two frames using the WAN 2.2 FLF2V model.",
      "acceptanceCriteria": [
        {
          "id": "US-007-AC01",
          "text": "`manifest()` returns exactly 3 `ModelEntry` items matching the **active** (non-bypassed)"
        },
        {
          "id": "US-007-AC02",
          "text": "The CLIP loader active in the workflow (`umt5_xxl_fp8_e4m3fn_scaled.safetensors`) is"
        },
        {
          "id": "US-007-AC03",
          "text": "`run(start_image, end_image, prompt, negative_prompt, width, height, length, *, models_dir, seed, steps, cfg) -> list[PIL.Image]` uses `WanFirstLastFrameToVideo` conditioning and two-pass `KSamplerAdvanced` (high-noise → low-noise), matching the workflow execution order."
        },
        {
          "id": "US-007-AC04",
          "text": "BYPASSED nodes are not included."
        },
        {
          "id": "US-007-AC05",
          "text": "A unit test verifies `manifest()` length and field names."
        },
        {
          "id": "US-007-AC06",
          "text": "An example script `examples/wan22_flf2v.py` demonstrates usage."
        },
        {
          "id": "US-007-AC07",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-008",
      "title": "WAN 2.2 sound-to-video pipeline (`s2v`)",
      "description": "As a Python developer, I want `comfy_diffusion.pipelines.video.wan.wan22.s2v` with `manifest()` and `run()` so that I can generate audio-driven video using the WAN 2.2 S2V 14B model.",
      "acceptanceCriteria": [
        {
          "id": "US-008-AC01",
          "text": "`manifest()` returns exactly 5 `ModelEntry` items matching active nodes in"
        },
        {
          "id": "US-008-AC02",
          "text": "`run(audio, ref_image, control_video, prompt, negative_prompt, *, models_dir, seed, steps, cfg) -> list[PIL.Image]` calls `load_audio_encoder`, `audio_encoder_encode`, then iterates the multi-pass `WanSoundImageToVideoExtend` + `KSampler` + `LatentConcat` loop as structured in the workflow's subgraph chain."
        },
        {
          "id": "US-008-AC03",
          "text": "The `audio` parameter accepts a `dict` with keys `\"waveform\"` (torch tensor) and"
        },
        {
          "id": "US-008-AC04",
          "text": "A unit test verifies `manifest()` length and field names with mocked loaders."
        },
        {
          "id": "US-008-AC05",
          "text": "An example script `examples/wan22_s2v.py` demonstrates usage."
        },
        {
          "id": "US-008-AC06",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-009",
      "title": "WAN 2.2 text-and-image-to-video pipeline (`ti2v`)",
      "description": "As a Python developer, I want `comfy_diffusion.pipelines.video.wan.wan22.ti2v` with `manifest()` and `run()` so that I can generate video conditioned on both a text prompt and an optional reference image using the WAN 2.2 TI2V 5B model.",
      "acceptanceCriteria": [
        {
          "id": "US-009-AC01",
          "text": "`manifest()` returns exactly 3 `ModelEntry` items matching active nodes in"
        },
        {
          "id": "US-009-AC02",
          "text": "`run(prompt, negative_prompt, width, height, length, *, start_image=None, models_dir, seed, steps, cfg) -> list[PIL.Image]` calls `wan22_image_to_video_latent`, `ModelSamplingSD3` (shift=8), `KSampler` (uni_pc, 20 steps, cfg=5), and `vae_decode`, exactly matching `video_wan2_2_5B_ti2v.json`."
        },
        {
          "id": "US-009-AC03",
          "text": "When `start_image` is `None`, an empty latent is used (no mask injected)."
        },
        {
          "id": "US-009-AC04",
          "text": "A unit test verifies `manifest()` length and field names with mocked loaders."
        },
        {
          "id": "US-009-AC05",
          "text": "An example script `examples/wan22_ti2v.py` demonstrates usage."
        },
        {
          "id": "US-009-AC06",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-010",
      "title": "Package wiring and example scripts",
      "description": "As a Python developer, I want the WAN 2.2 pipelines discoverable via the standard package structure so that I can import them with a predictable path.",
      "acceptanceCriteria": [
        {
          "id": "US-010-AC01",
          "text": "`comfy_diffusion/pipelines/video/wan/wan22/__init__.py` exists and exports"
        },
        {
          "id": "US-010-AC02",
          "text": "`comfy_diffusion/pipelines/video/wan/__init__.py` is updated to expose `wan22`."
        },
        {
          "id": "US-010-AC03",
          "text": "Each of `examples/wan22_t2v.py`, `examples/wan22_i2v.py`, `examples/wan22_flf2v.py`,"
        },
        {
          "id": "US-010-AC04",
          "text": "Typecheck / lint passes."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "** `ModelManager.load_audio_encoder(path: str | Path) -> Any` loads an audio encoder"
    },
    {
      "id": "FR-2",
      "description": "** `audio_encoder_encode(audio_encoder, audio: dict) -> Any` encodes a ComfyUI audio"
    },
    {
      "id": "FR-3",
      "description": "** `wan_sound_image_to_video(...)` wraps `WanSoundImageToVideo.execute()`, returning"
    },
    {
      "id": "FR-4",
      "description": "** `wan_sound_image_to_video_extend(...)` wraps `WanSoundImageToVideoExtend.execute()`,"
    },
    {
      "id": "FR-5",
      "description": "** `wan22_image_to_video_latent(vae, width, height, length, batch_size, *, start_image)` wraps `Wan22ImageToVideoLatent.execute()`, using `comfy.latent_formats.Wan22()` for output processing."
    },
    {
      "id": "FR-6",
      "description": "** All five pipelines follow the `manifest() / run()` contract established in `ltx2_t2v.py`."
    },
    {
      "id": "FR-7",
      "description": "** All pipeline `manifest()` functions must include only models from **active**"
    },
    {
      "id": "FR-8",
      "description": "** Pipeline `run()` execution order must mirror the workflow node execution order"
    },
    {
      "id": "FR-9",
      "description": "** All new code follows the lazy-import pattern — no `torch` or `comfy.*` at module"
    },
    {
      "id": "FR-10",
      "description": "** All tests must pass on CPU-only environments (mock model weights; no GPU needed)."
    },
    {
      "id": "FR-11",
      "description": "** `audio_encoders/` directory is registered as a known folder path in `_runtime.py`"
    }
  ]
}
