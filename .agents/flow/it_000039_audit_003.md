# Audit Report — Iteration 000039 / PRD 003
## Image-to-Video Generation via parallax-cli (ltx2, ltx23, wan21, wan22)

---

## Executive Summary

PRD 003 (Image-to-Video via parallax-cli) is **fully implemented**. The `create video` command in `packages/parallax_cli/src/index.ts` correctly adds the `--input` flag, validates file existence before spawning, selects the appropriate `i2v.py` script for all four model families, and applies all per-model flag-forwarding rules (`--input`→`--image`, `--cfg`→`--cfg-pass1` for ltx2, `--steps` omitted for ltx23). The shared `spawnPipeline` helper is reused throughout. TypeScript typecheck passes with zero errors.

---

## Verification by FR

| FR     | Assessment | Notes |
|--------|------------|-------|
| FR-1   | ✅ comply  | `.option("--input <path>", ...)` added to `create video` definition |
| FR-2   | ✅ comply  | `opts.input !== undefined` branch selects i2v; else falls back to `VIDEO_SCRIPTS[opts.model]` (t2v) |
| FR-3   | ✅ comply  | Dispatch table: ltx2→`ltx/ltx2/i2v.py`, ltx23→`ltx/ltx23/i2v.py`, wan21→`wan/wan21/i2v.py`, wan22→`wan/wan22/i2v.py` |
| FR-4   | ✅ comply  | `PARALLAX_REPO_ROOT` checked in `spawnPipeline`; exits code 1 with clear message if unset |
| FR-5   | ✅ comply  | `existsSync(opts.input)` checked before dispatch; prints `Error: input file not found: <path>` and exits 1 |
| FR-6   | ✅ comply  | `args.push("--image", opts.input)` for all four models when `opts.input !== undefined` |
| FR-7   | ✅ comply  | All per-model flag rules applied: ltx2 uses `--cfg-pass1`, ltx23 omits `--steps`, others verbatim |
| FR-8   | ✅ comply  | `Bun.spawn(["uv", "run", "python", scriptPath, ...args])` |
| FR-9   | ✅ comply  | `{ stdin: "inherit", stdout: "inherit", stderr: "inherit" }` |
| FR-10  | ✅ comply  | `process.exit(await proc.exited)` |
| FR-11  | ✅ comply  | `opts.modelsDir ?? process.env.PYCOMFY_MODELS_DIR`; exits 1 if neither present |
| FR-12  | ✅ comply  | Shared `spawnPipeline` helper used for all dispatch |

---

## Verification by US

| US     | Assessment | Notes |
|--------|------------|-------|
| US-001 | ✅ comply  | ltx2 i2v: correct script, --image, --cfg-pass1, all other flags forwarded, subprocess exit code propagated |
| US-002 | ✅ comply  | ltx23 i2v: correct script, --image, --steps omitted, all other flags forwarded |
| US-003 | ✅ comply  | wan21 i2v: correct script, --image, all flags verbatim |
| US-004 | ✅ comply  | wan22 i2v: correct script, --image, all flags verbatim |
| US-005 | ✅ comply  | --input listed in help, selects i2v when present, preserves t2v when absent, file-not-found guard, typecheck passes |

---

## Minor Observations

1. The `--cfg-pass1` / `--steps`-omission logic is applied unconditionally (both t2v and i2v paths for ltx2/ltx23), which is consistent with the scripts' interfaces. No regression introduced.
2. The `--input` option description hardcodes the four model names inline; this may need updating if new models are added in future iterations.
3. No automated tests cover the i2v dispatch paths. Per project convention this is intentional (tests written in the Refactor phase), but should be tracked as a follow-up.

---

## Conclusions and Recommendations

All 12 functional requirements and 5 user stories fully comply. Implementation is clean, DRY, and follows established patterns.

Recommended next steps:
1. **Tests (Refactor phase):** Add unit/integration tests for the i2v dispatch table and per-model flag-forwarding logic.
2. **Refactor dispatch logic:** Consider extracting the per-model flag-forwarding into a dedicated lookup table or helper to reduce the growing conditional chain in the `create video` action handler as more models are added.

---

## Refactor Plan

### High Priority

1. **Extract i2v script dispatch table** — Define a `VIDEO_I2V_SCRIPTS` constant (parallel to `VIDEO_SCRIPTS`) mapping model names to i2v script paths, replacing the inline ternary chain.
2. **Extract per-model flag builder** — Move the conditional `--cfg-pass1` / `--steps`-omit / `--image` logic into a `buildVideoArgs(opts, isI2v: boolean): string[]` helper function to improve readability and testability.

### Lower Priority

3. **Add tests** — Write tests in the Refactor phase covering:
   - i2v script selection per model
   - flag forwarding for ltx2 (--cfg-pass1), ltx23 (--steps omit), wan21/wan22 (verbatim)
   - `--input` file-not-found guard
   - t2v no-regression (--input absent)
