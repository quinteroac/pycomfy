{
  "goals": [
    "Expose REST endpoints for submitting all five inference operations (create image/video/audio, edit image, upscale image).",
    "Provide a job status endpoint for polling.",
    "Provide a Server-Sent Events (SSE) endpoint per job for real-time progress streaming.",
    "Provide job list and cancel endpoints.",
    "Mount the gateway router on the existing FastAPI app in `server/main.py`."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Submit inference jobs via REST",
      "description": "As an external consumer (web UI, script, webhook), I want to POST a job request and receive a job ID immediately so that I can track a long-running inference without keeping the HTTP connection open.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`POST /jobs/create/image` accepts a JSON body with at minimum: `model: str`, `prompt: str`, and optional generation fields; returns `{\"job_id\": \"<id>\", \"status\": \"queued\"}` within 200ms."
        },
        {
          "id": "US-001-AC02",
          "text": "`POST /jobs/create/video` accepts a JSON body with `model: str`, `prompt: str`, and optional `input: str` (image path for i2v)."
        },
        {
          "id": "US-001-AC03",
          "text": "`POST /jobs/create/audio` accepts a JSON body with `model: str`, `prompt: str`, and optional audio fields."
        },
        {
          "id": "US-001-AC04",
          "text": "`POST /jobs/edit/image` accepts a JSON body with `model: str`, `prompt: str`, `input: str` (input image path)."
        },
        {
          "id": "US-001-AC05",
          "text": "`POST /jobs/upscale/image` accepts a JSON body with `model: str`, `prompt: str`, `input: str`."
        },
        {
          "id": "US-001-AC06",
          "text": "All five endpoints call `submit_job()` from `server/submit.py` and return the job ID in < 200ms."
        },
        {
          "id": "US-001-AC07",
          "text": "All request bodies are validated by Pydantic models defined in `server/schemas.py`."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Job status polling",
      "description": "As a consumer, I want to call `GET /jobs/{job_id}` so that I can check the current status of a job without holding a streaming connection.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "`GET /jobs/{job_id}` returns a JSON object with fields: `id`, `status`, `created_at`, `updated_at`, and `result` (null or `JobResult`)."
        },
        {
          "id": "US-002-AC02",
          "text": "Returns HTTP 404 with `{\"detail\": \"job not found\"}` when the job ID does not exist."
        },
        {
          "id": "US-002-AC03",
          "text": "Status values match: `\"queued\"`, `\"running\"`, `\"completed\"`, `\"failed\"`, `\"cancelled\"`."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "SSE progress stream",
      "description": "As a consumer, I want to connect to `GET /jobs/{job_id}/stream` and receive real-time progress events so that I can display a live progress bar without polling.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "`GET /jobs/{job_id}/stream` returns a `text/event-stream` response using FastAPI's `StreamingResponse`."
        },
        {
          "id": "US-003-AC02",
          "text": "Each event is a JSON-encoded `PythonProgress` object emitted as `data: <json>\\n\\n`."
        },
        {
          "id": "US-003-AC03",
          "text": "When the job reaches `\"completed\"` or `\"failed\"`, a final event with `step: \"done\"` or `step: \"error\"` is emitted and the stream closes."
        },
        {
          "id": "US-003-AC04",
          "text": "If the job does not exist, the endpoint returns HTTP 404 immediately (no stream opened)."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "Job list and cancel",
      "description": "As a consumer, I want to list recent jobs and cancel a queued job so that I can manage the job queue from external tools.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "`GET /jobs` returns a JSON array of the 50 most recent jobs, each with `id`, `status`, `created_at`, `updated_at`."
        },
        {
          "id": "US-004-AC02",
          "text": "`DELETE /jobs/{job_id}` cancels a job that is in `\"queued\"` status; returns `{\"cancelled\": true}`."
        },
        {
          "id": "US-004-AC03",
          "text": "`DELETE /jobs/{job_id}` returns HTTP 409 with `{\"detail\": \"job is already running or completed\"}` when the job is not in `\"queued\"` status."
        }
      ]
    },
    {
      "id": "US-005",
      "title": "Health endpoint",
      "description": "As a deployment operator, I want `GET /health` to return service status so that load balancers and monitoring tools can verify the gateway is alive.",
      "acceptanceCriteria": [
        {
          "id": "US-005-AC01",
          "text": "`GET /health` returns HTTP 200 with `{\"status\": \"ok\", \"version\": \"<package_version>\"}`."
        },
        {
          "id": "US-005-AC02",
          "text": "The endpoint does not depend on the job queue being available (no DB call)."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "All gateway routes are defined in `server/gateway.py` as an `APIRouter` and mounted on the main FastAPI app in `server/main.py` with prefix `/` (or no prefix)."
    },
    {
      "id": "FR-2",
      "description": "All request/response schemas are Pydantic models in `server/schemas.py` with strict type annotations."
    },
    {
      "id": "FR-3",
      "description": "SSE streaming uses `asyncio` and polls the job queue every 500ms maximum; polling interval is configurable via `PARALLAX_SSE_POLL_MS` env var."
    },
    {
      "id": "FR-4",
      "description": "CORS is enabled on the gateway for `*` origins (configurable via `PARALLAX_CORS_ORIGINS` env var)."
    },
    {
      "id": "FR-5",
      "description": "The gateway runs on the same `uvicorn` instance as `server/main.py` — no new process or port."
    }
  ]
}
