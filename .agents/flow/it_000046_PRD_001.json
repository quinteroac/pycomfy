{
  "goals": [
    "Expose `ltx23/ia2v` through `parallax create video` via a new `--audio` flag.",
    "Keep the command surface backwards-compatible (existing t2v and i2v invocations are unaffected).",
    "Guard `--audio` so it is only accepted for `ltx23` (the only model that supports it)."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Add `--audio` option to `create video`",
      "description": "As a developer, I want to pass `--audio <path>` to `parallax create video` so that I can generate a video conditioned on an audio track without writing Python.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`parallax create video --model ltx23 --prompt \"...\" --input image.png --audio track.wav --output out.mp4` runs without error and produces a valid `.mp4` file."
        },
        {
          "id": "US-001-AC02",
          "text": "`--audio` is optional; omitting it leaves existing t2v / i2v behaviour unchanged for all models."
        },
        {
          "id": "US-001-AC03",
          "text": "If `--audio` is supplied with a model other than `ltx23`, the CLI prints `Error: --audio is only supported for model 'ltx23'.` to stderr and exits with code 1."
        },
        {
          "id": "US-001-AC04",
          "text": "If the path given to `--audio` does not exist, the CLI prints `Error: audio file not found: <path>` to stderr and exits with code 1."
        },
        {
          "id": "US-001-AC05",
          "text": "If `--audio` is supplied without `--input`, the CLI prints `Error: --audio requires --input (image).` to stderr and exits with code 1."
        },
        {
          "id": "US-001-AC06",
          "text": "Typecheck / lint passes."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Route to `ia2v` pipeline in the `ltx23` runner",
      "description": "As a developer, I want the `_ltx23` runner to call `ia2v.run()` when an audio path is provided so that the correct pipeline is executed end-to-end.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "`_ltx23` accepts an `audio` keyword argument (default `None`)."
        },
        {
          "id": "US-002-AC02",
          "text": "When `audio` is not `None`, `_ltx23` imports and calls `comfy_diffusion.pipelines.video.ltx.ltx23.ia2v.run()` with the correct arguments: `models_dir`, `image`, `audio_path`, `prompt`, `width`, `height`, `length`, `fps`, `cfg`, `seed`."
        },
        {
          "id": "US-002-AC03",
          "text": "When `audio` is `None` and `image` is set, the existing `i2v` path is unchanged."
        },
        {
          "id": "US-002-AC04",
          "text": "When both are `None`, the existing `t2v` path is unchanged."
        },
        {
          "id": "US-002-AC05",
          "text": "`RUNNERS[\"ltx23\"]` signature accepts `audio=None` without breaking existing call sites (uses `**_` for unknown kwargs)."
        },
        {
          "id": "US-002-AC06",
          "text": "Typecheck / lint passes."
        },
        {
          "id": "US-002-AC07",
          "text": "Visually verified in terminal: running the command produces a `.mp4` file."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "** `cli/commands/create.py` — add `audio: Annotated[Optional[str], typer.Option(\"--audio\", help=\"Input audio file for ia2v (ltx23 only).\")]` parameter (default `None`) to `create_video`."
    },
    {
      "id": "FR-2",
      "description": "** `cli/commands/create.py` — validate that `--audio` is only used with `--model ltx23`; error and exit otherwise."
    },
    {
      "id": "FR-3",
      "description": "** `cli/commands/create.py` — validate that the audio file exists on disk when provided; error and exit otherwise."
    },
    {
      "id": "FR-4",
      "description": "** `cli/commands/create.py` — validate that `--input` is also provided when `--audio` is given; error and exit otherwise."
    },
    {
      "id": "FR-5",
      "description": "** `cli/commands/create.py` — pass `audio=audio` to `video.RUNNERS[model](...)` (the runner ignores it via `**_` for models that do not use it)."
    },
    {
      "id": "FR-6",
      "description": "** `cli/_runners/video.py` — update `_ltx23` signature to `(*, mdir, prompt, image, w, h, n, f, s, c, seed, audio=None, **_)` and add ia2v branch."
    },
    {
      "id": "FR-7",
      "description": "** `cli/_runners/video.py` — the ia2v branch loads a PIL image from `input` path (already done by the caller), then passes `image` object + `audio_path=audio` to `ia2v.run()`."
    },
    {
      "id": "FR-8",
      "description": "** async mode — include `audio` in the `args` dict passed to `run_async` so the job payload is complete."
    }
  ]
}
