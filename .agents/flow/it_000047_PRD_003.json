{
  "goals": [
    "Let users install the frontend with a single command, consistent with the existing CLI module pattern.",
    "Store the frontend at a predictable, user-owned path (`~/.parallax/frontend/`).",
    "Persist the path so the server uses it without manual env var management."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Install the frontend via CLI",
      "description": "As a user, I want to run `parallax frontend install` so that the chat UI is downloaded and ready to be served by the Parallax server without any manual steps.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "Running `parallax frontend install` downloads the latest pre-built frontend archive from the project's GitHub Releases (asset name: `parallax-frontend-{version}.tar.gz`)."
        },
        {
          "id": "US-001-AC02",
          "text": "The archive is extracted to `~/.parallax/frontend/`, replacing any previous installation."
        },
        {
          "id": "US-001-AC03",
          "text": "After installation, `~/.parallax/config.env` (or equivalent config file used by `parallax install`) contains `PARALLAX_FRONTEND_PATH=~/.parallax/frontend`."
        },
        {
          "id": "US-001-AC04",
          "text": "The command prints: `Frontend installed at ~/.parallax/frontend` on success."
        },
        {
          "id": "US-001-AC05",
          "text": "If the download fails (network error, 404), the command prints a human-readable error and exits with a non-zero code — it does not leave a partial installation."
        },
        {
          "id": "US-001-AC06",
          "text": "Running the command a second time re-downloads and overwrites the existing installation cleanly."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "Pin a specific frontend version",
      "description": "As a user, I want to install a specific version of the frontend with `parallax frontend install --version 1.2.3` so that I can pin the UI to a known-good release.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "`--version` accepts a semver string (e.g. `1.2.3`)."
        },
        {
          "id": "US-002-AC02",
          "text": "If the specified version does not exist as a GitHub Release asset, the command prints the error returned by GitHub and exits non-zero."
        },
        {
          "id": "US-002-AC03",
          "text": "If `--version` is omitted, the latest release is installed."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "Check installed frontend version",
      "description": "As a user, I want to run `parallax frontend version` so that I can see which version of the frontend is currently installed.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "Running `parallax frontend version` prints the installed version string (read from `~/.parallax/frontend/version.txt`)."
        },
        {
          "id": "US-003-AC02",
          "text": "If no frontend is installed, it prints: `Frontend not installed. Run \\`parallax frontend install\\` to install.`"
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "A new `frontend` command group is added to the Typer CLI in `cli/main.py`, with subcommands `install` and `version`."
    },
    {
      "id": "FR-2",
      "description": "`parallax frontend install` downloads the release archive via HTTPS using `httpx` (already available via FastAPI deps) — no new network deps."
    },
    {
      "id": "FR-3",
      "description": "The archive is a `.tar.gz` containing the `dist/` contents at its root; extraction target is `~/.parallax/frontend/`."
    },
    {
      "id": "FR-4",
      "description": "After extraction, the command writes `PARALLAX_FRONTEND_PATH` to the Parallax config file (same file written by `parallax install`)."
    },
    {
      "id": "FR-5",
      "description": "A `version.txt` file is included in the release archive and contains the semver version string; `parallax frontend version` reads this file."
    },
    {
      "id": "FR-6",
      "description": "The GitHub Release asset URL pattern is: `https://github.com/{owner}/{repo}/releases/download/v{version}/parallax-frontend-{version}.tar.gz`."
    },
    {
      "id": "FR-7",
      "description": "The `.github/workflows/release-cli.yml` CI workflow is extended to build the frontend with Bun (`bun run build` in `frontend/`), package the `dist/` as `parallax-frontend-{version}.tar.gz`, and upload it as a release asset alongside the existing CLI binary."
    }
  ]
}
