{
  "goals": [
    "Implement a Typer-based CLI under `cli/` with the same command surface as `parallax_cli`.",
    "Support both sync (blocking) and `--async` (non-blocking, returns job ID) modes on all generation commands.",
    "Add a `parallax jobs` subcommand group with `list`, `watch`, `status`, `cancel`, and `open` sub-commands.",
    "Provide a `python -m parallax` entry point registered in `pyproject.toml`."
  ],
  "userStories": [
    {
      "id": "US-001",
      "title": "Generation commands (sync mode)",
      "description": "As a developer, I want to run `parallax create image --model sdxl --prompt \"...\"` and have it block until the output file is ready so that I can use it in scripts that depend on the result.",
      "acceptanceCriteria": [
        {
          "id": "US-001-AC01",
          "text": "`parallax create image`, `parallax create video`, `parallax create audio`, `parallax edit image`, `parallax upscale image` are implemented as Typer commands under `cli/commands/`."
        },
        {
          "id": "US-001-AC02",
          "text": "In sync mode (default, no `--async`), each command calls the pipeline `run()` directly (or `spawnPipeline` equivalent) and blocks until completion."
        },
        {
          "id": "US-001-AC03",
          "text": "On success, the command prints the output file path and exits with code 0."
        },
        {
          "id": "US-001-AC04",
          "text": "On failure, the command prints the error to stderr and exits with code 1."
        },
        {
          "id": "US-001-AC05",
          "text": "`--help` on each command shows all available options with descriptions."
        }
      ]
    },
    {
      "id": "US-002",
      "title": "`--async` flag on generation commands",
      "description": "As a developer, I want to append `--async` to any generation command so that the CLI returns a job ID immediately and I can continue working without waiting for inference.",
      "acceptanceCriteria": [
        {
          "id": "US-002-AC01",
          "text": "All five generation commands accept an `--async` flag (boolean, default False)."
        },
        {
          "id": "US-002-AC02",
          "text": "When `--async` is provided, the command calls `submit_job()` from `server/submit.py` instead of blocking."
        },
        {
          "id": "US-002-AC03",
          "text": "The command prints exactly: `Job <job_id> queued\\n  → parallax jobs watch <job_id>` and exits with code 0."
        },
        {
          "id": "US-002-AC04",
          "text": "When `--async` is NOT provided, behavior is identical to current sync mode."
        }
      ]
    },
    {
      "id": "US-003",
      "title": "`parallax jobs` subcommand group",
      "description": "As a developer, I want a `parallax jobs` subcommand so that I can list, monitor, and cancel jobs from the terminal.",
      "acceptanceCriteria": [
        {
          "id": "US-003-AC01",
          "text": "`parallax jobs list` prints a table of the 20 most recent jobs with columns: `ID`, `STATUS`, `MODEL`, `CREATED`."
        },
        {
          "id": "US-003-AC02",
          "text": "`parallax jobs status <job_id>` prints the full job record as formatted JSON."
        },
        {
          "id": "US-003-AC03",
          "text": "`parallax jobs watch <job_id>` renders a live progress bar (using `rich` or `typer`'s progress utilities) that updates until the job reaches a terminal state, then prints the output path or error."
        },
        {
          "id": "US-003-AC04",
          "text": "`parallax jobs cancel <job_id>` cancels a queued job and prints `Cancelled <job_id>` or an appropriate error."
        },
        {
          "id": "US-003-AC05",
          "text": "`parallax jobs open <job_id>` opens the output file with the OS default application (`xdg-open` / `open`)."
        }
      ]
    },
    {
      "id": "US-004",
      "title": "`python -m parallax` entry point",
      "description": "As a developer, I want to invoke the CLI with `python -m parallax` or `uv run parallax` so that I don't need a separate Bun install to use the CLI.",
      "acceptanceCriteria": [
        {
          "id": "US-004-AC01",
          "text": "`cli/__main__.py` exists and calls `app()` from `cli/main.py`."
        },
        {
          "id": "US-004-AC02",
          "text": "`pyproject.toml` registers a `parallax` script entry point pointing to `cli.main:app`."
        },
        {
          "id": "US-004-AC03",
          "text": "`uv run parallax --help` prints the top-level help text without error."
        },
        {
          "id": "US-004-AC04",
          "text": "`uv run python -m parallax --help` also works."
        }
      ]
    }
  ],
  "functionalRequirements": [
    {
      "id": "FR-1",
      "description": "CLI is implemented with Typer; `rich` may be used for table and progress output."
    },
    {
      "id": "FR-2",
      "description": "All CLI source lives under `cli/` at repo root; `cli/commands/` contains one file per command group."
    },
    {
      "id": "FR-3",
      "description": "The CLI reads `PARALLAX_RUNTIME_DIR`, `PYCOMFY_MODELS_DIR`, and `PARALLAX_UV_PATH` env vars with the same semantics as the current TS CLI."
    },
    {
      "id": "FR-4",
      "description": "Sync mode uses `comfy_diffusion.pipelines` directly — no subprocess spawning for sync execution."
    },
    {
      "id": "FR-5",
      "description": "`--async` mode uses `submit_job()` and never imports `torch` or `comfy.*` in the CLI process."
    },
    {
      "id": "FR-6",
      "description": "All Typer commands have explicit `--help` texts for every option."
    }
  ]
}
